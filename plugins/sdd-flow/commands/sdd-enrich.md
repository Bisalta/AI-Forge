---
description: Solo la fase de refinement — convierte una idea cruda en un requerimiento decision-closed (sin planear ni codear).
argument-hint: "<idea o tarea cruda>"
---

# /sdd-enrich — Decision-Closed Refinement

Invocá el skill **`enrich-user-story`** para: **$ARGUMENTS**

Forzá decisiones en las 6 dimensiones con `AskUserQuestion` (opciones clickeables). No escribas el artefacto final hasta que el usuario confirme. No codees. No planees todavía — esto solo cierra el requerimiento.

Salida: requerimiento con Story, Objective, Scope (in/out), **Closed decisions**, Expected behavior, Expected output, Success criteria.
