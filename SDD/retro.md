# Retro — el sistema aprende o repite

Una línea por evento que el ciclo no debería tener que redescubrir: `ESCALATE` resuelto, blocker repetido, prerequisito no documentado. Lo escribe el planner al cerrar cada ronda. En Feature Ready se relee: si un patrón se repitió, se propone el ajuste al doc que corresponda.

| # | Fecha | Ciclo · ronda | Qué pasó | Patrón | Ajuste propuesto |
|---|---|---|---|---|---|
| RT1 | 2026-08-13 | sicop-hardening · R0 r1 | `ESCALATE`: el contract v1 salió sin threat model ni bloque `concerns:`, y el requerimiento entregaba un control de seguridad (`secret-scan.sh`) sin un solo AC de seguridad encima. | El planner puede auto-aprobar un contract al que le falta una sección obligatoria de `security.md` §6, porque `sdd-lint-contract.sh` sólo mira frases abiertas y paths, no secciones ausentes. | Agregar al linter un chequeo de **secciones obligatorias presentes** (threat model, `concerns:`), no sólo de frases prohibidas. Un contract sin threat model debería salir rojo antes de auto-aprobarse, no en la ronda 1 del review. |
| RT2 | 2026-08-13 | sicop-hardening · R0 r1 | `ESCALATE`: AC3 exigía `shellcheck` limpio sobre todos los `.sh`, insatisfacible porque 3 hallazgos viven en `plugins/`, que el brief prohibía tocar. El agente lo cerró solo bajando el piso a `--severity=warning`. | El planner escribió un AC que cruza el límite de scope que él mismo declaró. Nadie lo verifica: el linter no sabe qué archivos toca el brief. | En el self-review de `sdd-plan`, chequear cada AC contra el out-of-scope del mismo requerimiento. Un AC que sólo se satisface tocando algo prohibido es un defecto de plan. |
| RT3 | 2026-08-13 | sicop-hardening · R0 r2 | `REJECTED`: el fix de un MAJOR introdujo un BLOCKER nuevo — para evitar un falso positivo, el agente excluyó `SDD/contracts/` entero del escaneo de secretos. | **La corrección introdujo el defecto**, que es exactamente el patrón de fondo del análisis de SICOP ("la mayoría de los defectos no entraron al escribir, entraron al corregir"). Confirma la regla de R3 sobre correcciones. | Ya cubierto por el AC24 de R3 (las correcciones vuelven al loop). Vale como evidencia de que la regla hace falta: acá la corrección se revisó porque estaba dentro del ciclo; fuera del ciclo no se habría revisado. |
| RT4 | 2026-08-13 | sicop-hardening · R0 r2 | La causa raíz del BLOCKER de RT3 era del plan: el contract citaba cuatro literales de credencial contiguos en AC6bis, así que el propio contract disparaba el detector. | Un contract que documenta fixtures de seguridad puede volverse indetectable-por-diseño para su propio gate. El implementador no tiene autoridad para arreglar el contract, así que su única salida barata es ablandar el gate. | Documentar en `sdd-plan` que las fixtures de credencial en un HLTC se escriben partidas (clave y valor en spans separados). Es la misma técnica que `plant_kv` usa en los tests. |
| RT5 | 2026-08-13 | sicop-hardening · R0 r1-r3 | El sello `Commit:` del reporte de gates mintió en las tres rondas (`e5ef90c`, `83c8bb2`, …): siempre el HEAD anterior al commit del trabajo. | El bug de R1 reproducido tres veces seguidas en el ciclo que existe para arreglarlo. No es anécdota: es la medición de que el modo de falla es sistemático, no un descuido. | Ninguno adicional — es literalmente el scope de R1. Se registra como evidencia de que el fix mecánico era la decisión correcta frente a "recordar regenerar". |
| RT7 | 2026-08-13 | sicop-hardening · R0 r1-r3, R1 r1 | **Tercera aparición** de la misma clase: una salida de comando pegada en un verification report queda desfasada del artefacto commiteado (4 asserts de 6 en R0 r1, un extracto etiquetado "pegada tal cual" en R0 r3, números de línea de un estado intermedio en R1 r1). Ninguna era falsa en su conclusión; las tres lo eran en el detalle. | El agente pega la evidencia **durante** el trabajo, y después el archivo sigue creciendo. El momento correcto de capturar es al final, contra el árbol que se va a commitear — que es el mismo problema de timing que R1 resolvió para el reporte de gates, sin resolver para la evidencia que escribe el modelo. | Regla en `quality-gates.md` §5: la salida pegada a mano se captura **después** del último cambio al artefacto que describe, y el reviewer la re-corre. Es el complemento humano del sellado mecánico de R1. Candidato a quinto cambio, más allá de las 4 propuestas de SICOP. |
| RT6 | 2026-08-13 | sicop-hardening · R0 | R0 consumió tres rondas y ~1,0 M tokens; la mayor parte se fue en `secret-scan.sh`, un gate que el `project-scaffold` declaró obligatorio. | El checklist de `project-scaffold` mete un gate de seguridad obligatorio en el mismo requerimiento que funda la escalera, y ese gate se come el presupuesto del requerimiento funcional que venía después. | Evaluar en `archetypes.md` si `project-scaffold` debería admitir el gate 9 como `N/A — se cierra en el primer requerimiento funcional`, o si conviene un arquetipo aparte. Decisión de Gabriel, no la tomo acá. |
