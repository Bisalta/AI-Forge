# Retro — el sistema aprende o repite

Una línea por evento que el ciclo no debería tener que redescubrir: `ESCALATE` resuelto, blocker repetido, prerequisito no documentado. Lo escribe el planner al cerrar cada ronda. En Feature Ready se relee: si un patrón se repitió, se propone el ajuste al doc que corresponda.

| # | Fecha | Ciclo · ronda | Qué pasó | Patrón | Ajuste propuesto |
|---|---|---|---|---|---|
| RT1 | 2026-08-13 | sicop-hardening · R0 r1 | `ESCALATE`: el contract v1 salió sin threat model ni bloque `concerns:`, y el requerimiento entregaba un control de seguridad (`secret-scan.sh`) sin un solo AC de seguridad encima. | El planner puede auto-aprobar un contract al que le falta una sección obligatoria de `security.md` §6, porque `sdd-lint-contract.sh` sólo mira frases abiertas y paths, no secciones ausentes. | Agregar al linter un chequeo de **secciones obligatorias presentes** (threat model, `concerns:`), no sólo de frases prohibidas. Un contract sin threat model debería salir rojo antes de auto-aprobarse, no en la ronda 1 del review. |
| RT2 | 2026-08-13 | sicop-hardening · R0 r1 | `ESCALATE`: AC3 exigía `shellcheck` limpio sobre todos los `.sh`, insatisfacible porque 3 hallazgos viven en `plugins/`, que el brief prohibía tocar. El agente lo cerró solo bajando el piso a `--severity=warning`. | El planner escribió un AC que cruza el límite de scope que él mismo declaró. Nadie lo verifica: el linter no sabe qué archivos toca el brief. | En el self-review de `sdd-plan`, chequear cada AC contra el out-of-scope del mismo requerimiento. Un AC que sólo se satisface tocando algo prohibido es un defecto de plan. |
| RT3 | 2026-08-13 | sicop-hardening · R0 r2 | `REJECTED`: el fix de un MAJOR introdujo un BLOCKER nuevo — para evitar un falso positivo, el agente excluyó `SDD/contracts/` entero del escaneo de secretos. | **La corrección introdujo el defecto**, que es exactamente el patrón de fondo del análisis de SICOP ("la mayoría de los defectos no entraron al escribir, entraron al corregir"). Confirma la regla de R3 sobre correcciones. | Ya cubierto por el AC24 de R3 (las correcciones vuelven al loop). Vale como evidencia de que la regla hace falta: acá la corrección se revisó porque estaba dentro del ciclo; fuera del ciclo no se habría revisado. |
| RT4 | 2026-08-13 | sicop-hardening · R0 r2 | La causa raíz del BLOCKER de RT3 era del plan: el contract citaba cuatro literales de credencial contiguos en AC6bis, así que el propio contract disparaba el detector. | Un contract que documenta fixtures de seguridad puede volverse indetectable-por-diseño para su propio gate. El implementador no tiene autoridad para arreglar el contract, así que su única salida barata es ablandar el gate. | Documentar en `sdd-plan` que las fixtures de credencial en un HLTC se escriben partidas (clave y valor en spans separados). Es la misma técnica que `plant_kv` usa en los tests. |
| RT5 | 2026-08-13 | sicop-hardening · R0 r1-r3 | El sello `Commit:` del reporte de gates mintió en las tres rondas (`e5ef90c`, `83c8bb2`, …): siempre el HEAD anterior al commit del trabajo. | El bug de R1 reproducido tres veces seguidas en el ciclo que existe para arreglarlo. No es anécdota: es la medición de que el modo de falla es sistemático, no un descuido. | Ninguno adicional — es literalmente el scope de R1. Se registra como evidencia de que el fix mecánico era la decisión correcta frente a "recordar regenerar". |
| RT11 | 2026-08-14 | sicop-hardening · R4 r2 | El arnés de medición del agente devolvió `127` en las **quince** corridas de sus cinco triples: puso el comando en una variable y lo invocó sin comillas, y `zsh` no hace word splitting, así que buscó un ejecutable llamado literalmente `bash SDD/tests/...`. El test nunca corrió. Lo detectó antes de pegar los exit codes. | **Quinta vez en el ciclo que una medición no medía lo que decía**, y la primera en el **instrumento** en vez de en el objeto — el caso difícil, porque un instrumento roto no se queja. Lo que lo destapó no fue auditar el arnés: fue que **las tres corridas del triple dieran el mismo número**. | Regla para `quality-gates.md` §10.3, propuesta por el propio agente: **un triple cuyas tres corridas no se distinguen entre sí no es un triple, es una medición muerta** — y ahí se sospecha del arnés antes que del control. Es la única forma de la clase `RT8` que se detecta **sin** saber nada del instrumento, así que es la que conviene escribir. |
| RT10 | 2026-08-14 | sicop-hardening · R4 r1 | `BLOCKED` correcto: dos de los siete ítems del checklist estadístico del arquetipo `analysis` eran **autosatisfacibles por el error que pretenden prevenir**. El ítem 5 se cumplía declarando la unidad equivocada, que es el error que motiva el ítem; el 6 pedía potencia sin decir contra qué efecto, y la potencia observada es una transformación monótona del p-valor. | **Es el único de los ocho defectos de plan del ciclo que un review por grep no podía encontrar.** Los otros siete se midieron: un payload, un `git ls-tree`, un conteo, una re-corrida. Éste sólo aparece leyendo el criterio como estadístico, o el día que alguien lo aplica a un dataset real y la conclusión sale mal igual. El review adversarial tiene un punto ciego estructural donde el criterio pertenece a un dominio que ni el planner ni el reviewer dominan. | Cuando un arquetipo o un checklist pertenece a un dominio que el planner no domina (estadística, criptografía, contabilidad, legal), el brief tiene que **pedir explícitamente la auditoría del criterio** y habilitar el `BLOCKED`. Acá funcionó porque se pidió. Sin pedirlo, el implementador lo habría implementado textual y el reviewer lo habría aprobado por grep. |
| RT9 | 2026-08-13 | sicop-hardening · R2 r1 | `ESCALATE`: el contract ordenó poner el bloque de identidad "tras el chequeo de rama protegida", y esa ubicación lo dejó aguas abajo de cuatro salidas tempranas, apagando el enforcement con `SDD_ALLOW_BASE_COMMIT=1` y en detached HEAD. El requerimiento que existe para matar una guarda tautológica introdujo una forma nueva de la misma tautología. | **El planner escribió una ubicación relativa ("tras X") sin mirar qué había entre X y el final.** Un Architectural Delta que ubica código por vecindad en vez de por la condición que tiene que cumplir hereda todas las salidas que haya en el medio. Tercer `ESCALATE` del ciclo y el tercero por defecto de plan, no de código. | En el self-review de `sdd-plan`: cuando el Delta ubica un bloque nuevo dentro de un archivo existente, la ubicación se declara por **la condición que debe valer al ejecutarse**, no por qué bloque tiene al lado. Junto con RT2 (AC que cruza su propio out-of-scope), son dos chequeos del mismo tipo: el planner no verifica sus propias afirmaciones contra el archivo real. |
| RT8 | 2026-08-13 | sicop-hardening · R1 + consolidado externo | **Cuatro artefactos del ciclo se apoyan en plumbing de git que responde "qué conoce git", no "qué hay".** `HEAD` (propuesta 2, arreglado en R1), el sha del último commit de un archivo (descartado por el consolidado externo: miente si el archivo está modificado sin commitear), `git ls-files` (D8: no ve untracked, produjo un verde falso medido) y `git stash create` (D7: no representa untracked). El consolidado externo lo detectó en 2 lugares; este ciclo encontró 2 más, por separado. | No es una coincidencia de cuatro bugs: es una clase. Cada vez que un artefacto de evidencia le pregunta a git en vez de mirar el árbol, hereda el mismo agujero — y el agujero es siempre "lo que todavía no se le declaró a git", que es exactamente el estado en que trabaja un agente. | Regla para `quality-gates.md` §5: **la evidencia que afirma cobertura sobre un conjunto de archivos declara cómo enumeró ese conjunto**, y las formas basadas en el índice de git llevan su límite escrito. Es la generalización de las propuestas 2 y 3 del consolidado, y sale de haber roto las dos. |
| RT7 | 2026-08-13 | sicop-hardening · R0 r1-r3, R1 r1 | **Tercera aparición** de la misma clase: una salida de comando pegada en un verification report queda desfasada del artefacto commiteado (4 asserts de 6 en R0 r1, un extracto etiquetado "pegada tal cual" en R0 r3, números de línea de un estado intermedio en R1 r1). Ninguna era falsa en su conclusión; las tres lo eran en el detalle. | El agente pega la evidencia **durante** el trabajo, y después el archivo sigue creciendo. El momento correcto de capturar es al final, contra el árbol que se va a commitear — que es el mismo problema de timing que R1 resolvió para el reporte de gates, sin resolver para la evidencia que escribe el modelo. | Regla en `quality-gates.md` §5: la salida pegada a mano se captura **después** del último cambio al artefacto que describe, y el reviewer la re-corre. Es el complemento humano del sellado mecánico de R1. Candidato a quinto cambio, más allá de las 4 propuestas de SICOP. |
| RT6 | 2026-08-13 | sicop-hardening · R0 | R0 consumió tres rondas y ~1,0 M tokens; la mayor parte se fue en `secret-scan.sh`, un gate que el `project-scaffold` declaró obligatorio. | El checklist de `project-scaffold` mete un gate de seguridad obligatorio en el mismo requerimiento que funda la escalera, y ese gate se come el presupuesto del requerimiento funcional que venía después. | Evaluar en `archetypes.md` si `project-scaffold` debería admitir el gate 9 como `N/A — se cierra en el primer requerimiento funcional`, o si conviene un arquetipo aparte. Decisión de Gabriel, no la tomo acá. |
| RT12 | 2026-08-25 | consumo GEN-101 · R4+R2+R3 | **El contract se ratificó cinco veces en un ciclo de cuatro requerimientos, y las cinco salieron del self-review del planner o de un solo AC — ninguna del review de un agente.** v2: nombres de test que `run.sh` no habría descubierto. v3: falso positivo sobre el contract de GEN-94. v4: un artefacto listado para `haiku` en la prosa y ausente del Delta. v5: un AC que se marcaba con la regla que implementa. | Confirma con más fuerza el hallazgo de GEN-94 (6 de 8 defectos eran del plan): **el artefacto menos verificado del ciclo es el que escribe el planner**, y el planner es el único que no tiene a nadie encima. Los cinco se detectaron ejecutando, no leyendo. | Los cuatro chequeos que R4 automatiza atacan la forma del contract, no su contenido. La clase que queda viva —un AC satisfacible pero equivocado— sólo aparece al correrlo. Candidato: que el brief exija correr cada AC contra el artefacto real antes de despachar, no después. |
| RT13 | 2026-08-25 | consumo GEN-101 · R2 | **Tercera aparición de «la herramienta se pone roja sobre la prosa que describe lo que prohíbe»** — antes `sdd-check.sh` (D11) y `guard-git.sh` (D10), ahora el grep de AC10 sobre la sección que enuncia la regla de tier. | Deja de ser anécdota: cualquier chequeo por literal sobre un repo que **documenta** sus propias prohibiciones se marca solo. No es un bug de cada herramienta, es una propiedad de escribir la regla y el detector en el mismo árbol. | Escribir el patrón una vez en `quality-gates.md`: **todo chequeo por literal declara cómo distingue el uso de la mención**, y la exclusión va por línea (lenguaje de prohibición en la misma línea), nunca por archivo — excluir archivos ciega el chequeo, que es D6. `sdd-lint-contract.sh` ya tiene la forma correcta; falta generalizarla. |
| RT14 | 2026-08-25 | consumo GEN-101 · R4 | De los nueve ACs de R4, **el único que encontró un defecto fue AC6** — el que corre contra el contract de GEN-94, un artefacto real que nadie escribió para ese test. Los otros ocho corren contra fixtures escritas junto con el código que prueban. | Una fixture escrita por quien escribe el código hereda sus supuestos: si el autor cree que los concerns se declaran en YAML, la fixture los declara en YAML y el test pasa. El artefacto ajeno es el único que trae supuestos que el autor no tenía. | Proponer para `quality-gates.md` §3: cuando un AC afirma que algo **no** se marca (ausencia de falso positivo), al menos uno de sus tests corre contra un artefacto preexistente del repo, no contra una fixture del propio brief. Barato y fue lo único que pagó acá. |
| RT15 | 2026-08-25 | consumo GEN-101 · R1 | El triple de mutación de AC19 falló en la primera corrida porque **predije el exit code del mutante** (0) y salió 1: al quitar la validación de rol, `FILES` queda sin asignar y `set -u` corta antes. La mutación era la declarada; lo equivocado era mi expectativa del resultado. | Es RT11 visto desde el otro lado. RT11 dice que tres corridas idénticas son una medición muerta; esto dice que **una expectativa escrita de antemano sobre el mutante también es una predicción, no una medición**. El triple vale porque compara corridas entre sí, no contra un número que alguien adivinó. | En `quality-gates.md` §10: el assert del triple se escribe sobre **la pérdida de la detección** (deja de cumplirse la condición de aprobación del AC), no sobre el valor concreto que devuelve el mutante. Un mutante puede fallar de una forma distinta a la prevista y seguir probando lo mismo. |
| RT16 | 2026-08-25 | consumo GEN-101 · revisión externa (Esteban Fait, Slack) | La palanca D del diseño («sesiones en paralelo», 44% del consumo) se aceptó tal cual del panel de uso, sin mecanismo propio — a diferencia de A, B y C, que traían desglose o script. La revisión externa encontró que la cifra puede ser un **confound**: si una persona concentra buena parte del gasto de la organización y también corre más en paralelo, «44% ocurre en paralelo» describe dónde está esa persona, no una causa independiente. | Misma clase que RT8/RT11 (medición que no mide lo que dice medir), aplicada por primera vez a una cifra de un panel externo en vez de a un script del propio repo. Y confirma el costo concreto de saltear el `reviewer-agent`: el ciclo corrió inline (ver ejecución de R1-R4), así que el trabajo adversarial que lo habría encontrado no ocurrió — lo hizo un humano en su lugar. | Ninguna palanca entra a un diseño con una cifra de panel sin mecanismo propio, aunque el panel sea de Anthropic. Antes de tratar un número reportado como accionable: desglose, control de confound (quién concentra el uso) y verificación independiente — el mismo estándar que A/B/C, no una excepción para D por parecer obvio. Demovida a hipótesis sin verificar en el diseño; no se implementó nada sobre ella. |
| RT17 | 2026-08-26 | consumo GEN-101 · segunda revisión externa (Esteban Fait, Slack) | Dos huecos operativos en el plan de R5: (1) "el número de GEN-94" salió de arqueología manual y nada garantizaba que ciclos futuros lo reprodujeran sin que alguien se acordara de rehacerla; (2) "un par de ciclos" no tenía fecha, y con sólo 3 repos con SDD completo y 3 personas, semanas sin que nadie lo note no sería raro. | Es la contraparte operativa de RT16: RT16 encontró un dato sin mecanismo (la palanca D); esto encuentra un **plan de acción** sin mecanismo — "vuelvo con el número" sin nada que lo dispare. Ambos son la misma familia: una promesa que suena verificable pero depende de que alguien recuerde. | Resuelto con R6: `SDD/escalations.md` (ledger, Clase cerrada, escrita en el mismo acto de resolver el evento — no después) + `sdd-escalation-tally.sh` (cuenta, no clasifica) + tarea Proxima con fecha real. El propio arnés de R6 reprodujo la lección en miniatura: el primer intento de fixture para AC24 (reconstruir una fila por campos con `awk` sobre contenido UTF-8) produjo una fila que no matcheaba el patrón del tally — 0 filas inválidas detectadas, no porque el script estuviera sano sino porque la fixture estaba rota. Sustituir por un literal conocido lo arregló. Ajuste: cuando una fixture reconstruye una fila de tabla con contenido no-ASCII, preferir sustitución literal sobre un texto exacto conocido antes que reensamblado por campos. |
| RT18 | 2026-08-27 | consumo GEN-101 · ronda 1 del reviewer, REJECTED | El primer AC de detección que se escribió en este ciclo (AC19, R1) tenía la pata "revertido" del triple fabricada — reusaba `$rc_bad` de la primera corrida en vez de ejecutar una tercera vez. Es exactamente el defecto que RT11 y RT15 (de este mismo ciclo, escritos ANTES) existen para prevenir. | Escribir la regla no inmuniza contra cometer el error — el conocimiento de "los triples tienen que ser corridas reales" estaba documentado dos veces en el propio `retro.md` y no impidió una tercera instancia, porque nadie corrió el propio arnés contra sí mismo hasta que una revisión externa lo hizo. | Ninguna regla nueva — el patrón ya está escrito (RT11, RT15). Lo que falta no es más documentación, es que el self-review incluya "romper cada mutante a mano y mirar el número real", no sólo leer que el código dice que lo hace. |
| RT19 | 2026-08-27 | consumo GEN-101 · ronda 1 del reviewer, REJECTED | R6 construyó un mecanismo (`SDD/escalations.md` + `sdd-escalation-tally.sh`) para que otros dos repos lo usaran, y nunca lo agregó a la lista de scripts que `/sdd-init` distribuye. El checkpoint `GEN-102` que depende de esto quedó agendado sobre una premisa falsa. | Crear un artefacto nuevo y crear su canal de distribución son dos pasos distintos, y el primero se siente como "terminado" sin el segundo. Nada en el checklist del arquetipo `infra` preguntaba explícitamente "¿esto llega a donde tiene que llegar?" — preguntaba por rollback y config, no por propagación. | Candidato para `standards/archetypes.md`, checklist `infra`: un ítem explícito "si este cambio crea un artefacto que otro repo debe consumir, ¿el canal de distribución existente ya lo incluye?". No se agrega en este ciclo — es una propuesta, no una decisión tomada. |
| RT20 | 2026-08-27 | consumo GEN-101 · ronda 2 del reviewer, segunda REJECTED consecutiva | 5 de los 7 `MAJOR` de la ronda 2 fueron el mismo patrón: un valor se corrigió en el código (una versión de script, un conteo de eventos, un nombre de archivo renombrado) y NO se propagó a todas las citas de ese valor en el contract — el AC que lo declaraba, el binding que lo referenciaba, la nota de ratificación que lo resumía. Cinco lugares distintos, el mismo defecto. | Un `.replace()` puntual arregla el lugar donde se miró; no arregla los lugares que no se buscaron. La corrección de la ronda 1 fue grande (10+ archivos) y se hizo con reemplazos dirigidos, sin un barrido exhaustivo de "¿dónde más se cita este valor viejo" antes de dar por cerrado cada hallazgo. | Antes de cerrar cualquier fix que cambie un valor citado en más de un lugar (versión de script, conteo, nombre de archivo): `grep` del valor VIEJO sobre el árbol completo, no sólo sobre el archivo que se está editando. Aplicado a partir de la ronda 3 de este mismo ciclo — ver los commits de esa ronda. |
| RT21 | 2026-08-27 | consumo GEN-101 · ronda 2 del reviewer, MAJOR M4 + M5 | Dos hallazgos de la misma ronda resultaron ser la misma lección en dos formas: (a) el fix de la subestimación de R1 (ronda 1) sólo cubrió la forma de cita que el hallazgo original mostraba (`standards/x.md` prefijado), y quedó una forma pelada (`x.md` a secas) sin cubrir — cerrar la instancia encontrada, no la clase. (b) El mecanismo de R6 no se aplicó sobre su propio primer evento calificado (el `REJECTED` de la ronda 1), en el mismo commit que lo construyó. | Un fix verificado contra el caso que lo motivó puede seguir siendo un fix parcial si el caso motivador no agotaba la clase de defecto. Y una herramienta de tracking nueva no se prueba sola con "el código compila" — se prueba usándola sobre el primer evento real disponible, que en este caso era el propio ciclo que la estaba construyendo. | Al cerrar un hallazgo de "faltó cubrir la forma X", preguntar explícitamente "¿hay una forma Y que tampoco cubrí?" antes de declarar cerrado — no esperar a que una ronda siguiente la encuentre. Y cuando un ciclo construye un mecanismo de tracking, ejercitarlo sobre los eventos que el propio ciclo ya generó antes de darlo por terminado. |
| RT22 | 2026-09-01 | fix-check-self-scoping · hallazgo externo (Bisalta/WMS-Back, ciclo WMS-34) | Un ciclo SDD en otro repo, corriendo la copia cacheada de `sdd-check.sh` (0.10.0), midió tres defectos combinados: (a) D11 seguía abierta — el script se autodetecta; (b) el guard `.md` de las tres reglas built-in (test-skipeado/supresor/no-verify) nunca se extendió al loop de patrones custom de `sdd-check.patterns`, así que un reporte de evidencia commiteado que CITA el literal de un patrón custom como parte de lo que encontró se vuelve una violación nueva — y como los reportes se acumulan sin sobreescribirse (R1, R2, R3...), el falso positivo sólo crece ("eco creciente"). Al reproducir (b) con un fixture de test, apareció una tercera forma no reportada: el propio `SDD/scripts/sdd-check.patterns`, recién agregado, se autodetecta contra su propia regla — misma clase que (a), un nivel más abajo. | Tercera vez que este ciclo encuentra "form Y" al cerrar "form X" (mismo patrón que RT21): el guard `.md` se escribió una vez (AC42) y nunca se propagó al loop hermano que se agregó después, y la auto-exclusión de D11 nunca se generalizó a los demás artefactos que un checker por literal necesariamente contiene (su propio patterns file). Confirma RT13: "escribir la regla y el detector en el mismo árbol" no es un bug de una herramienta, es una propiedad de la clase. | `sdd-check.sh` 0.11.0: auto-exclusión por path (precedente `secret-scan.sh`) aplicada al propio script Y al `PATTERNS_FILE` activo, más el guard `.md` extendido al loop de patrones custom — las tres reglas built-in y el loop custom comparten ahora el mismo mecanismo de exclusión. Test de regresión: `SDD/tests/test_check_self_scoping.sh` (casoA/B/C). Pendiente, no resuelto acá: la generalización completa que RT13 propone (exclusión por línea con lenguaje de prohibición, no por archivo/extensión) sigue sin aplicarse — el fix extiende el patrón `.md` existente, no lo reemplaza. |
| RT23 | 2026-09-18 | bisalta-db GEN-108 · validación del retorno de R1 por el planner | El contract v1 citaba **"los doce archivos de test preexistentes"** en `AC40` y en el impact set. Hay **14**. El número no salió de contar: salió de copiar la frase "12 archivos de test tras el ciclo `GEN-101`" de `SDD/docs/doc_quality_gates.md`, que quedó obsoleta cuando el árbol creció. El defecto entró al contract, al brief de R2 y habría entrado al trabajo de R2 si no aparece al validar el retorno de R1. | **Cuarta confirmación de RT12**: el artefacto que escribe el planner es el menos verificado, y el planner es el único sin nadie encima. Pero la forma es nueva y peor que la de RT20: RT20 es *no propagar* una corrección a las otras citas del valor; esto es **importar un valor ya podrido desde un doc de gobierno y volverlo normativo**. El doc era la fuente autorizada, y por eso no se verificó — la autoridad del origen sustituyó a la medición. | Ratificado en v2, y la corrección **no es escribir 14**: es dejar de congelar el número. `AC40` se verifica derivando del árbol (`git diff --name-only` sobre `SDD/tests/test_*.sh`), y `AC38` absorbe la corrección del doc con su propia mutación. Regla candidata para `sdd-plan`: **una cifra que el contract copia de otro documento se re-mide en el acto de copiarla, o no se copia — se deriva**. Un doc de gobierno es una fuente con fecha, no una constante. |
| RT24 | 2026-09-18 | bisalta-db GEN-108 · Fase 3, creación de subtareas | `standards/orchestration.md` y el prompt de `/sdd` afirman, dos veces y en imperativo, que **"las subtasks NO devuelven key (solo UUID)"**, y de ahí deriva la regla de que la rama use el key de la tarea madre. Al crear `GEN-108.1` y `GEN-108.2` con `proxima_create_task`, las dos **devolvieron key propia**. | Una afirmación sobre el comportamiento de un sistema externo quedó escrita como invariante del protocolo y nunca se re-verificó contra el sistema. Es la misma clase que RT23 un nivel más afuera: no es una cifra podrida de un doc propio, es una **capacidad de un tercero** que cambió sin que el protocolo se enterara. La regla que deriva de ella (rama = key de la madre) sigue siendo buena por otras razones —una rama por ciclo, no por subtarea— pero hoy se justifica con una premisa falsa. | No se cambia el protocolo en este ciclo: la regla de branching se mantiene, porque su razón real es que el ciclo integra por un PR y no por subtarea. Lo que sí hay que corregir es **la justificación**, en `orchestration.md` y en `commands/sdd.md`: decir "la rama usa el key de la madre porque el ciclo integra completo", no "porque las subtareas no tienen key". Registrado como deuda; fuera del scope de GEN-108. |
| RT25 | 2026-09-18 | bisalta-db GEN-108 · R1, rondas 1→3 del reviewer | **Tres rondas, y las tres rechazaron la misma clase de defecto en artefactos distintos**: un procedimiento que declara esperar un resultado que no puede producir. Ronda 1: `LEFT JOIN … ON 1=0` (devuelve `'NO'` para las 35 bases con o sin aprovisionamiento) y `psql -f` sin `ON_ERROR_STOP=1` (sale 0 aunque todo falle). Ronda 2: el `INSERT` de prueba sobre `sys.tables`/`information_schema.tables`, que falla por vista no actualizable **antes** del chequeo de ACL, y un user de scratch sin `db_datareader`, que probaba "un principal sin roles no puede escribir". Ronda 3: cerrada. | Es la misma familia que `v0.11.0` ("la evidencia no puede mentir por descuido") pero desplazada: acá los ocho ACs son `manual-only` y **su única cobertura es prosa ejecutable**. Un test roto lo atrapa el runner; un *procedimiento* roto no lo atrapa nadie hasta que una persona lo corre contra una base real — y para entonces el ciclo ya cerró. La revisión adversarial fue el único mecanismo que los encontró, y los encontró leyendo semántica de SQL, no corriendo comandos. | Candidato para `quality-gates.md` §2: un AC `manual-only` exige que el contract declare, además de la razón, **cuál es el resultado observable que distingue el éxito del fallo** — no "falla con error de permiso", sino "falla con `permission denied` y NO con `cannot insert into view`". La mitad de los hallazgos de este ciclo habrían sido imposibles de escribir con esa exigencia. |
| RT26 | 2026-09-18 | bisalta-db GEN-108 · R1 ronda 3, hallazgo del planner confirmado por el reviewer | **El runner y la regla de mutación se pisan.** `sdd-run-gates.sh:272` trunca el archivo de `-o`; `quality-gates.md` §10.3 exige pegar los triples **a mano en ese mismo archivo**; y el encabezado que el runner estampa dice que editarlo a mano invalida la evidencia. Las tres reglas son del mismo plugin y no pueden ser verdad a la vez. Se manifiesta en cada ronda de review, que es exactamente cuando el runner se vuelve a correr. | Ninguna de las tres reglas está mal por separado, y por eso sobrevivió tres ciclos sin que nadie la viera: el conflicto sólo aparece cuando un brief tiene evidencia manuscrita **y** más de una ronda. `GEN-94` y `GEN-101` la tuvieron y no la reportaron — hay que asumir que se perdió evidencia y nadie lo notó, que es peor que el bug. | Registrado como `D34`, dueño `sdd-flow`, fuera del scope de GEN-108. Dos salidas posibles: modo append-preserving en el runner (preservar todo lo que siga a un marcador `# Addendum`), o separar la evidencia manuscrita a un archivo hermano que el runner nunca toque — y en cualquiera de los dos casos, ajustar el texto del encabezado, que hoy afirma una invariante que el propio flujo obliga a violar. |
| RT27 | 2026-09-18 | bisalta-db GEN-108 · R2 ronda 1, BLOCKER del reviewer | **El verification report nunca pasa por el gate de seguridad.** El runner corre la escalera y sella el árbol; el addendum con los triples de mutación se pega **después** y se commitea aparte. Medido: el árbol sellado `ae9262` (commit `ee4a5ae`) **no contenía el report** (`git show ee4a5ae:…R2.md` → `fatal: exists on disk, but not in`), y el árbol entregado tenía `secret-scan.sh` en **exit 1** por dos literales del propio addendum. El gate 9 verde era cierto sobre un árbol que por construcción excluye el archivo que lo rompe. | Es `D34` (el runner trunca el archivo donde §10.3 manda pegar a mano) llevado a su consecuencia real, y es peor que perder evidencia: **un secreto pegado en la evidencia llega a `prod` sin que ningún gate lo vea**. Y no es hipotético — pasó, en el archivo que menos lo puede tener, y el mismo agente había diagnosticado la clase correctamente dos secciones antes de reproducirla. El orden "correr gates → pegar evidencia → commitear" hace que el último commit del ciclo sea siempre el menos verificado. | Cierre de esta ronda: correr `secret-scan.sh` sobre el árbol **final post-addendum ya commiteado** y declarar esa corrida en el report, con la comprobación positiva de que el report está dentro del árbol medido — sin esa tercera línea, un verde es indistinguible del que la ronda 1 declaró de buena fe. Mecanismo pendiente en `D34`, dueño `sdd-flow`: o el runner preserva por append, o la evidencia manuscrita va a un archivo hermano que el runner no toca — y en cualquier caso **el gate tiene que correr después de pegarla**, no antes. |
| RT28 | 2026-09-18 | bisalta-db GEN-108 · R2 ronda 2, hallazgos del propio agente durante el fix | Dos instancias nuevas de "la medición que no mide lo que dice medir", aparecidas **mientras se arreglaba la anterior**: (a) la sección del report que explicaba por qué el literal rompía el gate lo volvió a romper al citarlo — cuarta aparición del patrón RT13 en este repo; (b) el `grep -c 'Addendum de'` que verificaba que el addendum estuviera presente empezó a devolver 2 en vez de 1 en cuanto el report citó ese mismo comando dentro de un bloque de código: seguía siendo no-cero y seguía "pasando", pero ya no contaba encabezados. | (b) es la forma más fina que apareció hasta ahora: un chequeo que **sigue pasando por la razón equivocada**. No se rompió, se volvió insensible — y un no-cero no distingue "hay un encabezado" de "hay una cita de mí mismo". Ninguna de las dos las encontró el reviewer: las encontró el agente ejecutando, que es la misma lección de RT12 y RT14 (el defecto aparece al correr, no al leer). | (b) corregido con ancla `^#`. Regla que sale, candidata para `quality-gates.md` §10: **un chequeo por conteo declara qué cuenta y por qué ese número y no otro**; `-c` sin ancla sobre un archivo que puede citarse a sí mismo es un chequeo que sólo verifica que el archivo no esté vacío. Y la generalización de (a), cuarta vez: **todo archivo que documenta un gate por literal necesita la convención de partir el literal desde el primer borrador, no después de que el gate se ponga rojo.** |
| RT29 | 2026-09-18 | bisalta-db GEN-108 · planner, tres veces el mismo renglón | El encabezado `## Acceptance criteria (IDs del contract vN …)` de `SDD/briefs/R1-infra-accesos-lectura.md` quedó desactualizado **tres veces seguidas**: nació en v1, lo corregí a v2 tras el bump de v2, quedó en v2 tras el bump a v3, y la ronda 2 de R2 lo volvió a encontrar. Cada corrección fue puntual y ninguna atacó la causa. | RT20 dice "grepeá el valor viejo antes de cerrar"; lo apliqué y aun así reincidí, porque el problema no es el barrido sino **que la cita existe**. Los IDs de AC son estables entre versiones por diseño (lo dice el propio `sdd-plan`), así que nombrar la versión en ese encabezado no aporta nada y crea una obligación de mantenimiento que se incumple en cada bump. La versión sí importa en el header del brief, que es **una** línea y tiene dueño. | Resuelto por eliminación, no por corrección: el encabezado ya no nombra versión ("IDs del contract — estables entre versiones"). Regla candidata para `sdd-plan`: **la versión del contract se cita una sola vez por artefacto, en su header; el resto del documento la referencia sin número.** Una cita que no aporta información es una cita que sólo puede envejecer mal. |
| RT30 | 2026-09-18 | bisalta-db GEN-108 · R1 ronda 5, `ESCALATE` | **El contract cambió una decisión de diseño y no reconcilió los ACs que la verifican.** El v6 sacó `SSISDB` del loop de SQL Server; `AC7` y `AC42` siguieron diciendo "todas las bases de usuario en línea", y `SSISDB` lo es. El procedimiento entregado assertaba la negación de la letra de su propio AC: quien lo ejecutara leyendo el contract lo marcaba rojo, leyendo el runbook lo marcaba verde. | **Quinta confirmación de RT12 en este ciclo**, y la más incómoda: el propio contract fija esta disciplina **dos veces** — "sin AC no se prueban" (v4 punto 1) y el nombramiento explícito del AC afectado (v4 punto 9) — y el planner no la aplicó a su propia decisión de v6. Escribir la regla en el artefacto no basta para cumplirla al editar ese mismo artefacto. Y el costo fue real: consumió la tercera ronda de un cap de tres, así que el agente se quedó sin vueltas por un defecto que no era suyo. | Regla candidata para `sdd-plan`, mecanizable: **todo bump de contract que cambie una decisión de diseño declara, en el mismo acto, qué ACs quedan afectados — aunque la respuesta sea "ninguno", escrita.** Es el mismo chequeo de sección obligatoria que `sdd-lint-contract.sh` ya hace para `concerns:` y el Checklist del arquetipo: un bloque `acs-afectados:` por cada sección de cambios de versión, ausente = BLOCKER. Sin eso, la disciplina depende de que el planner se acuerde, y este ciclo prueba que no se acuerda. |
| RT31 | 2026-09-18 | bisalta-db GEN-108 · al registrar la escalación E9 | **Un test del propio harness assertaba una cifra congelada contra un artefacto cuyo propósito es crecer.** `test_escalation_ledger.sh` exigía "exactamente 8 filas", "TOTAL 8" y "plan 8" sobre `SDD/escalations.md`, y además su fixture de mutación usaba el ID `E9` "porque el ledger real ya tiene E1-E8". Registrar la novena escalación —el uso normal del mecanismo— rompió **seis asserts** sin que nada estuviera mal. | Es la misma clase que RT23 (la cifra copiada de un doc de gobierno) y que el bump v1→v2 de este ciclo, pero un nivel más adentro: acá la cifra congelada está **en el test**, o sea en el artefacto que existe para detectar drift. Un test así no es una aserción, es una fecha de vencimiento: pasa hasta que el sistema se use como fue diseñado. Y el mecanismo de `escalations.md` se construyó en GEN-101 precisamente para que registrar eventos no dependiera de que alguien se acordara — un test que castiga registrarlos empuja en la dirección contraria. | Corregido sustituyendo la cifra por la **invariante**, que es estrictamente más fuerte: los ocho eventos del backfill siguen presentes **uno por uno** (`E1`…`E8`, cada uno con Clase `plan`), **toda** fila lleva una Clase del enum cerrado, y el `TOTAL` del tally se compara contra el conteo derivado del propio ledger. La fixture pasa a `E999`. Verificado por mutación: vaciar la Clase de `E1` pone el test en rojo (0 → 1 → 0). Regla que sale: **un test sobre un artefacto acumulativo asserta propiedades de sus filas y relaciones derivadas, nunca un total literal.** |
| RT32 | 2026-09-21 | bisalta-db GEN-108 · mediciones de Patrick Ocampo | **Un identificador se leyó mal durante todo el ciclo y nadie lo notó, en los dos lados.** `cfrl3owqzwof` no es un cluster: es el sufijo DNS de la cuenta de AWS. Siete versiones de contract y el inventario "medido" lo trataron como identificador de cluster. En esa cuenta hay cuatro Aurora, no uno, y como los roles son objetos de cluster, "lectura en todas las BD" eran cuatro decisiones y se estaba tomando una. Lo mismo del lado de producción, donde además la cuenta nunca se enumeró. | El inventario se hizo bien —`SELECT datname` corrido de verdad, cifras contadas y no estimadas— y aun así describía un universo equivocado, porque **el error no estaba en la medición sino en qué se creía estar midiendo**. Ninguna de las salvaguardas del ciclo lo podía atrapar: el linter chequea closure, el runner chequea gates, el reviewer chequea que la evidencia reproduzca. Todas verifican que la respuesta sea correcta; ninguna verifica que la pregunta lo sea. Lo encontró la persona que fue a correr el script en el sistema real. | Regla candidata para `sdd-plan`: **cuando el contract nombra un recurso de infraestructura por un identificador, declara cómo se verificó que ese identificador designa lo que se cree** — un `describe-db-clusters`, un `SELECT`, la consola. Un token copiado de un endpoint no es un identificador verificado. Y el corolario del ciclo, más incómodo: **un inventario correcto sobre el alcance equivocado se ve exactamente igual que uno correcto.** |
| RT33 | 2026-09-21 | bisalta-db GEN-108 · mediciones de Patrick Ocampo | **Las dos heurísticas que este ciclo usó para marcar riesgo de datos fallaron, cada una en su dirección.** Por tamaño: `portalrh_qa` pesa 55 MB y tiene 3.458 planillas completas con cédulas y salarios; el contract usaba el peso (395 GB, 177, 107) como indicio de "esto es producción". Por nombre: `rrhh` —señalada en el contract y en el mensaje a Patrick porque "por nombre suena a nómina"— está **vacía**. Y en el otro cluster, las tres bases que la Parte 0 frenó por decir "producción" eran las tres más chicas, mientras las que pesaban no lo decían. | Es la generalización de RT13 (uso vs mención) aplicada a metadatos: **el nombre y el tamaño son lo que el sistema dice de sí mismo, no lo que contiene.** El ciclo entero razonó sobre proxies porque mirar adentro exigía un acceso que todavía no existía — o sea que la decisión de qué acceso dar se tomó sin el dato que sólo ese acceso da. No es evitable del todo; sí es declarable. | Para `standards/security.md`, threat model pregunta 4 ("¿qué datos toca y de quién?"): **si la respuesta se apoya en nombre, tamaño o ambiente declarado, el contract lo dice explícitamente y lo marca como pendiente de verificación adentro.** La guarda por nombre se declara freno, nunca clasificador — ningún artefacto puede concluir "acá no hay producción" a partir de ella. |
| RT34 | 2026-09-21 | bisalta-db GEN-108 · corrección de Patrick Ocampo al procedimiento de baja | R1 documentó el procedimiento de baja como *"correr el inverso con la lista todavía completa y recién después editarla"*. Patrick lo rechazó: **"depende de que alguien recuerde el orden"**. Su reemplazo cambia la fuente de verdad según la dirección de la operación — **se concede desde una lista explícita, se revoca por enumeración**: el inverso no lee la lista, recorre `sys.databases` buscando dónde existe el user y lo saca de donde lo encuentre. | La regla que sale es más general que el caso: **para conceder querés que mande la declaración, porque lo que nadie pidió no debe entrar; para revocar querés que mande la realidad, porque hay que encontrar lo que quedó donde nadie lo anotó.** Dos direcciones, dos fuentes de verdad. Y cubre el caso que ni el procedimiento de R1 ni el primero de Patrick alcanzaban: un user que quedó de antes de que la lista existiera. El defecto de R1 no era el orden de los pasos: era haber usado la misma fuente de verdad para las dos direcciones. | Candidato para `standards/archetypes.md`, checklist `infra`: **cuando un cambio se aplica desde una declaración, su reverso se verifica contra el estado real, no contra la misma declaración.** Un procedimiento de reverso que lee la misma lista que el de alta sólo funciona mientras la lista no haya cambiado — y la lista existe justamente para cambiar. |
| RT35 | 2026-09-21 | bisalta-db GEN-108 · verificación de Patrick Ocampo sobre su propio `REVOKE` | R1 sacó `portalrh_dev` y `portalrh_qa` del catálogo porque Ian no podía garantizar qué se conectaba con un rol distinto de `rh`. Patrick fue y lo midió: el delta de transacciones de tres días dio **idéntico** en las cuatro bases (96.375), y la serie de `portalrh.log` por día muestra que las escrituras **pararon el 17-sep**, un día antes de su `REVOKE` del 18 a las 16:50. Descartó además la hipótesis del fin de semana con datos (los sábados sí tienen filas) y encontró la explicación: siete días clavados en 36 filas no es gente, es una tarea automática, y el 17-sep es la fecha del pase de Hermes dev → Nexa prod. | **Las dos lecturas obvias del primer número eran falsas.** "Delta idéntico ⇒ el revoke cortó el tráfico" y "delta idéntico ⇒ es fin de semana" eran las dos hipótesis naturales, y ninguna sobrevivió a mirar la serie por día. El dato que decidió no fue el agregado sino la **forma temporal**: un valor constante durante siete días es una firma de máquina, y un corte que precede a la causa la descarta. | La lección operativa, que vale más que el caso: **cuando un número agregado admite dos explicaciones, la serie temporal desempata y el agregado no.** Y la de proceso: sacar las dos bases del catálogo *sin* poder responder la pregunta fue la decisión correcta —no dar una garantía que no se puede medir— y la medición posterior la confirmó sin revertirla. Las dos siguen fuera. |
| RT36 | 2026-09-21 | bisalta-db GEN-108 · ronda 1 de v10, ADVISORY del reviewer | **Segunda vez en el mismo ciclo que el planner cambia el diseño y no le da AC a la decisión central.** v10 movió el inverso de "leer la lista" a "revocar por enumeración" —la decisión más importante de esa versión— y su única cobertura era **incidental**: el verde de cierre de la mutación de `AC7` habría sido imposible si el inverso leyera la lista. Eso no es un AC, es un efecto colateral que nadie escribió a propósito. | Es literalmente el defecto que causó el `ESCALATE` de v6 → v7, y para entonces ya estaba escrito **dos veces** en este mismo contract ("sin AC no se prueban", y el nombramiento explícito del AC afectado en v4 punto 9). `RT30` propuso el mecanismo —un bloque `acs-afectados:` obligatorio por sección de cambios— y no se implementó, así que la disciplina siguió dependiendo de que el planner se acordara. **No se acordó, otra vez, cinco versiones después.** | Refuerza `RT30` con evidencia de reincidencia: la regla escrita en prosa no bastó ni siquiera para quien la escribió. El mecanismo propuesto —`sdd-lint-contract.sh` exigiendo un bloque `acs-afectados:` en cada sección de cambios de versión, ausente = BLOCKER— pasa de candidato a **necesario**. Mientras tanto, `AC43` cubre la revocación por enumeración. |
| RT37 | 2026-09-21 | bisalta-db GEN-108 · ronda 1 de v10, MAJOR 3 del reviewer | **El barrido de clase del agente no alcanzó `CHANGELOG.md` porque sus dos greps estaban acotados**: uno buscaba los nombres de los `.sql` (el changelog no los nombra) y el otro se limitaba a `plugins/bisalta-db/`. El changelog vendía `db_denydatawriter` como garantía vigente — el inverso exacto de lo que v10 decide — y **está en el impact set del propio contract**. | El barrido mejoró mucho desde la ronda que contestaba "ninguna otra instancia": ahora enumera recetas y las audita. Pero **el alcance del grep se eligió por vecindad** —los archivos del plugin, los que nombran los scripts— y no por la propiedad que se está barriendo, que es "todo artefacto que afirme algo sobre `db_denydatawriter`". Un barrido correcto sobre el recorte equivocado se ve igual que uno correcto: es `RT32` otra vez, un nivel más abajo. | Regla que sale, para el prompt de barrido de clase: **el alcance del grep se deriva del impact set del contract, no del directorio donde vive el cambio.** Si el contract lista `CHANGELOG.md` en su impact set, el barrido lo incluye aunque el cambio no lo haya tocado. |
| RT38 | 2026-09-21 | bisalta-db GEN-108 · R1 ronda 3 de v11, `ESCALATE` | **El barrido de clase se escribió con los patrones de las instancias ya corregidas, no con el concepto.** El MAJOR de la ronda 2 señaló tres citas de "reintentar el inverso" en `RUNBOOK.md`; el agente las corrigió y después barrió buscando *"correr"* sobre `RUNBOOK.md` y `grep|confirmable|sin salida` sobre los `.sql`. **Nunca grepeó `reintent` sobre los `.sql`**, que es donde vivía la cuarta — el `PRINT` que el operador recibe por stdout en el momento del evento, y la única de las cuatro que alguien lee bajo presión. | Es la tercera forma de la misma familia en este ciclo: `RT32` (medir bien el universo equivocado), `RT37` (alcance del grep elegido por vecindad de directorio) y ahora **alcance elegido por vecindad de patrón**. Las tres se ven idénticas a un barrido correcto desde afuera, y las tres producen una afirmación de cierre falsa —acá, literalmente: *"no queda ninguna remediación que deshaga un paso anterior"*—, que es justo la afirmación que el review tenía que poder dar por buena. El defecto no está en barrer poco: está en **derivar el patrón de los ejemplos que te dieron en vez de la propiedad que tenés que garantizar**. | Regla para el prompt de barrido de clase, que reemplaza a la de `RT37` en vez de sumarse: **el barrido se define por el concepto y se corre sobre toda la entrega**. En concreto: el término que se grepea sale del enunciado del defecto (acá, "reintentar"), no de las líneas que se acaban de corregir; y el alcance es el árbol del cambio completo, `.sql` incluidos, no el archivo donde estaban las instancias señaladas. Si el enunciado no da un término grepeable, eso mismo es la señal de que hay que escribir la propiedad antes de barrer. |
| RT39 | 2026-09-21 | bisalta-db GEN-108 · al registrar la escalación E10 | **El enum que el planner escribió en el test que verifica el enum no era el enum.** Al corregir el conteo congelado de `test_escalation_ledger.sh` (RT31), el planner reemplazó la cifra por una invariante —"toda fila lleva una Clase del enum cerrado"— y **escribió el enum de memoria**: `(plan\|ejecución\|herramienta\|decisión)`. El real, en `SDD/escalations.md`, es `(plan\|decisión\|medición\|otro)`. Pasó verde durante todo el ciclo porque las ocho filas del backfill son `plan`. Lo destapó el propio planner al registrar `E10` con Clase `ejecución` — que no existe — y ver el tally salir 3. | Es RT31 en su propia corrección: el fix del valor congelado introdujo **otro valor congelado, un nivel más arriba y más peligroso**, porque un enum equivocado en el chequeo de enum no falla cuando el enum se usa mal, falla cuando se usa **bien** — una fila legítimamente `medición` habría contado como sin clasificar. El chequeo habría empezado a mentir el día que alguien usara el ledger como fue diseñado, igual que el conteo congelado. | Corregido derivando el enum del propio documento (`sed` sobre las viñetas de la sección "Clase" de `escalations.md`), con mutación verificada: una fila con clase fuera del enum pone el test en rojo (0 → 1 → 0). La regla, que ya estaba en RT31 y no se aplicó a su propia corrección: **si el valor vive en un documento, el test lo lee del documento.** Un enum, un conteo y una ruta son la misma clase de cosa. |
| RT40 | 2026-09-22 | bisalta-db GEN-108 · verificación del cliente de SQL Server | **Casi reporto un hallazgo grave sobre una medición mía mal hecha.** Corrí `sqlcmd ... 2>&1 \| head -3; echo "exit=$?"` y leí `exit=0` con la conexión rechazada — iba a reportar que el cliente no devuelve código de error, que habría invalidado `AC3`, `AC42` y toda la premisa de exit codes del runbook. El `0` era el de `head`, no el de `sqlcmd`. Sin el pipe, sale 1. | Es la clase de este ciclo entero —`RT32`, `RT37`, `RT38`: medir bien algo que no es lo que se cree medir— cometida por el planner **mientras verificaba prerequisitos para cerrar**. Y el modo de falla es el más barato de todos: un pipe entre el comando y la lectura del código. Lo que lo salvó no fue una regla, fue haber corrido la variante sin pipe por otro motivo. | Regla operativa, corta y grepeable: **el exit code de un comando que va a un pipe no es el suyo.** Medirlo exige `PIPESTATUS[0]`, `set -o pipefail`, o no usar el pipe. Vale para cualquier verificación de este repo, y en particular para los triples de mutación, donde el exit code ES la medición. Anotado en `INVENTARIO.md` junto a la verificación que lo destapó. |
| RT41 | 2026-09-22 | bisalta-db GEN-108 · triple de mutación del `secret_id` | **Dos mutaciones seguidas fallaron en poner rojo un assert correcto, y las dos veces el defecto era mío.** Al derivar el `secret_id` del catálogo en `test_servidor_mcp.sh`, el triple exigía comprobar que el assert mide. Primer intento: quité el identificador del texto del mensaje de error → siguió verde. Segundo: quité el campo estructurado → siguió verde. Recién el tercero, quitando **los dos**, lo puso rojo. | La propiedad que el AC afirma —*"el error nombra el identificador del secreto"*— **viaja por dos caminos redundantes**: un campo estructurado y el texto del mensaje. Cada mutación individual quitaba un **portador**, no la **propiedad**, así que el assert seguía siendo cierto y tenía razón en quedarse verde. Es el reverso de `RT15`: ahí se predijo mal el exit code del mutante; acá se predijo mal **qué mutación falsifica el AC**. Y la redundancia no es un defecto — es lo que hace que el mensaje siga siendo útil aunque cambie el shape de la respuesta. | Regla para `quality-gates.md` §10.2, al declarar una mutación: **la mutación se elige contra la propiedad que el AC enuncia, no contra la primera línea de código que la produce.** Si la propiedad tiene más de un portador, la mutación los quita todos, o el AC se parte en uno por portador. Una mutación que deja la propiedad en pie no prueba que el control esté muerto: prueba que la mutación estaba mal elegida, y confundir las dos cosas lleva a "arreglar" un test que funcionaba. |
| RT42 | 2026-09-22 | bisalta-db GEN-108 · cierre del punto NEO por Patrick Ocampo | **La decisión de dos roles sobrevivió cuatro días y trece versiones de contract sin que nadie preguntara si el segundo consumidor existía.** Nació en el refinement del 18-sep como una buena idea —que `pg_stat_activity` distinga quién corrió qué, y poder revocarle a uno sin el otro—, Patrick la elogió (*"no se me había ocurrido y es claramente correcto"*), y quedó en el contract, en el runbook, en los scripts y en el README. Al preguntarle directo a NEO: **no abre ninguna conexión Postgres**, ni hoy ni en su diseño futuro; lee Odoo por XML-RPC, contra la aplicación. Y corre en la cuenta de producción, no en la de dev. | El razonamiento era correcto y la premisa no se verificó nunca. Es la misma familia que `RT32` —el inventario bien hecho sobre el universo equivocado— pero aplicada a un **consumidor** en vez de a un recurso: se diseñó el aislamiento entre dos partes sin confirmar que la segunda fuera parte. Y nada del proceso lo iba a atrapar: el rol se creaba bien, el script era idempotente, los ACs pasaban. Un rol de más no rompe nada — sólo deja una clave que rotar y, peor, **una pista falsa de que los dos mundos están conectados**. | Para `enrich-user-story`, dimensión de actor: **cuando el requerimiento nombra más de un consumidor, cada uno se confirma con su dueño antes de que entre al diseño** — no al final, cuando ya está en cinco artefactos. La pregunta es de una línea (*"¿esto lo va a usar X?"*) y acá habría ahorrado cuatro días de arrastre en ocho archivos. |
| RT43 | 2026-09-22 | bisalta-db GEN-108 · ronda 1 de v13, `contract-change-request` de `AGENT_r1` | **Tercera reincidencia del mismo defecto del planner, y esta vez lo atrapó el implementador.** v13 decidió el rol único y no reconcilió `AC1` (*"existen los roles `claude_lectura` y `neo_lectura`"*) ni la justificación de `AC29`. `AGENT_r1` lo detectó, **no lo trabajó alrededor**, abrió un `contract-change-request` y lo documentó en el runbook y en la evidencia respetando el single-writer. | Lo que falló ya está diagnosticado dos veces —`RT30` y `RT36`, con el mecanismo propuesto— y volvió a pasar. Lo nuevo es **quién lo encontró**: las dos veces anteriores fue el reviewer, una de ellas agotando la cota y terminando en `ESCALATE`. Acá lo levantó el agente al chocar con la contradicción mientras implementaba, que es el punto más barato de la cadena para detectarlo y el que el protocolo prevé (*"decisión no resuelta → BLOCKED → pregunta al planner, NO adivina"*). El costo bajó de una ronda entera de review a un párrafo en el retorno. | La regla no cambia — sigue siendo `RT30`: **todo bump que cambie una decisión de diseño declara qué ACs quedan afectados, aunque la respuesta sea "ninguno", escrita.** Lo que cambia es la evidencia de que la prosa no alcanza: tres reincidencias, una con `ESCALATE`. El bloque `acs-afectados:` obligatorio en `sdd-lint-contract.sh` deja de ser candidato y pasa a ser **lo único que queda por probar**, porque el resto ya se probó y falló. |
| RT44 | 2026-09-22 | bisalta-db GEN-108 · review de v14, tres MAJOR contra el planner | **El brief seguía instruyendo, bajo el título "Decisiones de diseño (cerradas — no las re-abras)", a crear el rol que `v13` había eliminado.** Y dos cosas lo agravan: (a) ese mismo brief ya tenía el mecanismo de derogación aplicado a sus puntos 3 y 6 —tachado más "derogado en vN"—, así que no faltaba la herramienta sino usarla; (b) el contract v11 **afirmaba** que el brief había quedado corregido (*"Corregido a v10"*), cuando sólo se habían tocado dos de sus puntos. La corrección declarada era más amplia que la hecha. | Lo primero es peligroso de una forma específica: un brief es lo que lee el **próximo agente**, y ese título le dice explícitamente que no lo cuestione. Una instrucción derogada bajo un rótulo de "cerrada, no re-abrir" es peor que una instrucción ausente. Lo segundo es la clase de `RT23` —una afirmación de cierre más ancha que el cierre— aplicada al propio changelog del contract, que es donde uno va a buscar qué se hizo. | Dos reglas, las dos para `sdd-plan`: **(1)** cuando un bump deroga algo que vive en un brief, se deroga **en el brief**, no sólo en el contract — el brief es la instrucción operativa y el contract es la referencia; **(2)** una línea de changelog que afirma "corregido X" nombra **qué** de X quedó corregido, o no lo afirma. La verificación de las dos es la misma que ya falta: el bloque `acs-afectados:`, extendido a `artefactos-afectados:`. |
| RT45 | 2026-09-22 | bisalta-db GEN-108 · primera prueba contra el secreto real | **Una instrucción correcta, pero legible en una sola dirección.** El `RUNBOOK.md` definía el campo `password` del secreto como *"la misma contraseña real que se sustituyó en el placeholder de la parte A al ejecutarla"*, y ponía la creación de los secretos como paso 10 de 10. Patrick Ocampo creó el secreto de Postgres **antes** de correr la Parte A — el orden inverso, igual de válido y nunca prohibido por el runbook — y con esa redacción la instrucción quedó apuntando a un evento que todavía no había ocurrido. | Familia nueva, vecina de RT20 y RT23 pero distinta: no es un valor podrido ni una cita sin propagar, es una **propiedad relacional escrita como secuencia temporal**. La relación real entre los dos valores es una igualdad —da igual cuál se fijó primero— y el texto la codificó como "A, y después B toma el valor de A". Una secuencia se rompe cuando alguien ejecuta en otro orden; una igualdad no. Agrava que el modo de falla es **mudo**: dos contraseñas distintas producen `FATAL: password authentication failed`, byte por byte el mismo error que un rol inexistente, porque Postgres le contesta a un rol que no existe con un desafío SCRAM falso (*mock authentication*, >=10) para impedir la enumeración de usuarios. La redacción defectuosa induce un error indistinguible del estado normal previo. | Corregido en `047643c`: la tabla define `password` como **el mismo valor** que el placeholder, los dos órdenes quedan escritos con la dirección de la que sale la contraseña en cada uno, y el paso 10 dice explícito que no depende de los anteriores. Regla candidata para `sdd-plan` y para los runbooks: **cuando dos artefactos tienen que coincidir, el doc escribe la igualdad, no el orden en que se escriben.** Un orden en un runbook es una comodidad de lectura; si además carga la única definición de un valor, se vuelve un requisito encubierto. |
| RT46 | 2026-09-22 | bisalta-db GEN-108 · diagnóstico de conexión | **Un script escrito para discriminar causas traía dos checks que no medían nada.** (a) El chequeo de TCP envolvía `/dev/tcp` en `timeout`, que **no existe en macOS** (es de GNU coreutils): sin el binario el comando falla siempre, así que ese check habría reportado `FALLA` con el puerto abierto, cerrado o inexistente. (b) Leía el campo `motor` del catálogo cuando se llama `dialecto`, e imprimió vacío sin protestar. | Sexta instancia de la clase que abrió RT11 —*la medición que no mide lo que dice medir*— y la primera en una herramienta cuyo propósito declarado es **distinguir causas**: un falso negativo ahí no es ruido, es exactamente el daño que venía a evitar. Lo que lo salvó no fue disciplina sino **redundancia accidental**: el check dijo "el puerto no abre" y tres líneas abajo `psql` recibió un `FATAL` del servidor, que sólo existe sobre una sesión TCP establecida. Dos mediciones del mismo hecho se contradijeron y la contradicción fue visible en la misma pantalla. Sin esa superposición, el script habría mentido en silencio. Y el segundo defecto es más callado todavía: **un campo ausente se imprime igual que un campo vacío**, así que nada lo delata. | Los dos corregidos: sonda TCP con `net.Socket` de Node (ya es dependencia) y el nombre real del campo, más un corte temprano si el dialecto no es `postgres`. La sonda se verificó **contra los cuatro estados que tiene que distinguir** —`abierto` (5432 real), `timeout` (5433 real), `ECONNREFUSED` (localhost:1), `ENOTFOUND` (host inventado)— antes de confiar en ella. Regla que sale, más angosta y más accionable que "probar los checks": **un check cuyo camino de falla depende de un binario externo verifica primero que ese binario exista**, porque su ausencia y la condición que busca producen la misma salida. |
| RT47 | 2026-09-22 | bisalta-db GEN-108 · corrección del runbook | **Escribí un puntero a una sección que no podía contestar la pregunta, dentro del commit que corregía esa misma clase de defecto.** Al documentar la ambigüedad de `password authentication failed` referencié *"ver Verificación de AC1 más abajo"*. La sección existe, es correcta y trata del rol — pero **todos sus comandos se conectan como `claude_lectura`**, incluida su consulta a `pg_roles`. Si el rol no autentica, ninguno corre: la sección presupone resuelto justo lo que estaba en duda. | Es un puntero roto que **ningún linter de paths puede detectar**, porque el destino existe y es del tema correcto. Lo que falla es la aplicabilidad: el destino tiene una precondición que el origen no cumple. Distinto de RT23 (valor podrido) y de RT44 (prosa derogada que sigue instruyendo) — acá nada está desactualizado ni es falso, y aun así la referencia no sirve. Y apareció **en el acto de corregir la misma familia**, que es la tercera vez en el repo (RT3, RT31) que una corrección trae adentro el defecto que corrige. | Bloque nuevo en el runbook —*"Si la conexión falla con `password authentication failed`"*— con la consulta a `pg_roles` que **requiere credencial administrativa**, dicho explícito, y una tabla que mapea cada resultado a su causa. La nota de la sección de revocación, que ya conocía la ambigüedad desde antes y no se contradecía, quedó enlazada al mecanismo. Regla candidata: **una referencia a un procedimiento de verificación declara con qué credencial o en qué estado corre**; sin eso, "ver la sección X" afirma una aplicabilidad que nadie comprobó. |
| RT48 | 2026-09-22 | bisalta-db GEN-108 · primera consulta contra el motor real | **`AC29` estaba verde en la suite y rojo en la base.** `current_setting('application_name')` devolvía `psql`: el plugin fijaba el valor con `-c application_name=…` dentro de `PGOPTIONS`, y psql pone el suyo al conectar y le gana. Medidas las tres formas lado a lado, sólo `PGAPPNAME` y el parámetro del conninfo sobreviven. El test sustituía `psql` por un stub que registraba el entorno, y assertaba que `PGOPTIONS` **contuviera** `application_name=<usuario>`. Lo contenía. | Séptima instancia de RT11 y la más cara: sobrevivió a **17 triples de mutación y a un reviewer adversarial**, porque ninguno tenía un motor enfrente. La mutación no podía atraparlo: quitar el `-c` ponía el test en rojo, o sea que el test **sí mataba a su barrera** — la barrera era la equivocada. La regla del ciclo (*si una barrera no tiene un test que la mate al quitarla, no cuenta*) es necesaria y no alcanza: prueba que el test discrimina **la forma**, no que la forma produzca **el efecto**. Un stub que registra argumentos verifica el contrato con la herramienta como uno lo imagina, no como la herramienta se comporta. | Corregido en el ciclo corto de fixes de v15: el valor va por `PGAPPNAME`, y el test lleva **dos** asserts — el positivo sobre `PGAPPNAME` y uno **negativo** que prohíbe `application_name` en `PGOPTIONS`, porque la forma rota se ve correcta y un assert positivo sobre la buena pasaría igual con la mala al lado. Cada mutación enrojece exactamente uno de los dos. Verificado contra la base: `app = claude_lectura`. Regla que sale: **un AC cuyo efecto vive en un sistema externo necesita al menos una verificación contra ese sistema**; el stub prueba que mandamos lo que creemos que hay que mandar, y sólo el sistema dice si era eso. Candidato para `quality-gates.md`: marcar esos ACs como `efecto-externo` y exigir su evidencia en vivo antes de Feature Ready, no después. |
| RT49 | 2026-09-23 | bisalta-db GEN-108 · al bumpear el contract a v15 | **Una versión citada sin medirla, dos veces en dos días, por dos manos distintas.** (a) El commit `2529071` corrigió el brief de R1 y escribió en el contract que eso ocurrió *"recién en v15"* — sin bumpear: el contract quedó en v14 citando una versión inexistente. (b) Veinticuatro horas después, en el resumen de sesión di el contract por "v15", y al escribir este fix puse **"contract v16"** en cinco comentarios de código y test, sin mirar el header. Lo detecté porque la línea 48 y el header no coincidían. | RT23 con otro valor: un número que se copia en vez de medirse. Lo específico es que **un número de versión parece metadato y no dato**, así que nadie lo trata como algo que haya que verificar — y sin embargo es una afirmación sobre el estado del mundo que se puede falsear igual que una cifra. Agrava que el error propio salió del resumen propio: el planner confió en su propia narración de hace unas horas antes que en el archivo. Y dentro del mismo fix hubo un tercero de la misma familia: escribí que la deuda de propiedad era `D45` cuando es `D42` — un ID citado de memoria. | Los tres corregidos antes del commit: v16 → v15 en los cinco lugares, la línea 48 reescrita para nombrar el commit en vez de la versión fantasma, D45 → D42. Propuesta concreta para `sdd-lint-contract.sh`: **toda cita `vN` dentro de un contract tiene que cumplir `N ≤` la versión del header**, y todo `D<n>` / `RT<n>` / `AC<n>` citado tiene que existir en su ledger. Los tres son chequeos mecánicos, baratos, y habrían atrapado los tres casos de hoy sin depender de que alguien lea con cuidado. |
| RT50 | 2026-09-23 | bisalta-db GEN-108 · al medir para cerrar el `ESCALATE` de v15 | **La lista blanca, que el diseño presentaba como la guarda del SQL, dejaba pasar escrituras en los dos dialectos, y nada del ciclo lo vio.** La regla era "cada sentencia empieza con `SELECT` o `WITH`", y un `WITH` puede llevar un `INSERT`/`DELETE`/`UPDATE`/`MERGE`; en SQL Server `WITH c AS (…) DELETE FROM c` es directamente un `DELETE`, y `SELECT … INTO` crea una tabla. Sobrevivió a R2 con **17 triples de mutación verificados**, a dos reviewers adversariales y a la review de v15. Lo encontró el planner midiendo si la afirmación del diseño era cierta, no revisando un diff. | **La disciplina de mutación prueba que un test detecta que la barrera se quitó; no prueba que la barrera cubra la amenaza.** Los doce casos de la lista blanca se portaron literales de `Bisalta/Proveedores-Back`, y con ellos se portó su punto ciego: todos los triples ponían rojo un caso enumerado, y nadie enumeró el CTE con escritura. Es RT48 en otro eje: RT48 fue un test que verificaba la forma y no el efecto; esto es un conjunto de tests que verifica bien lo que enumera y no enumera la amenaza. Lo que salvó a Postgres fueron tres barreras detrás —medido con cinco sondas sin efecto posible—, una de ellas (`ALTER ROLE … SET default_transaction_read_only`) sin documentar en el repo. SQL Server no tenía nada detrás salvo el rol, y se encontró **antes** de que existiera el login. Y al diseñar la guarda de réplica apareció la otra mitad de la lección: una segunda condición (`transaction_read_only`) que en una réplica **no puede fallar** nunca; se descartó antes de escribirla, porque su mutación habría quedado verde para siempre. | v16 lo cierra con `AC47` (rechazo de escritura embebida, con mutación que prueba que la normalización va antes) y `AC46` (la réplica comprobada en cada consulta, con parte `manual-only` contra el endpoint de escritura). Regla candidata para `security.md` y `sdd-plan`: **la respuesta del threat model a "¿qué pasa con input hostil?" enumera, por dialecto, las formas sintácticas que pueden escribir, y cada una tiene su caso en el test** — la enumeración es parte del contract, no del test, porque el test sólo puede probar lo que alguien pensó. Y para la regla de RT49: un chequeo mecánico de "todo `AC` citado existe" tiene que aceptar IDs retirados en las secciones "Cambios" (`AC45` se partió en tres en v16). |
