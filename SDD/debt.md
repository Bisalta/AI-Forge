# Deuda técnica — ledger

Registro append-only de deuda **conocida y aceptada**. Acá cae lo que el ciclo SDD decidió no arreglar ahora: `MINOR` de review que no se corrigieron, gates `N/A` con razón, vulnerabilidades de dependencias sin fix aplicable, `manual-only` que deberían automatizarse. Si no se registra, la deuda es invisible hasta que vuelve como incidente.

Reglas:
- Escriben el planner y el reviewer (al cerrar un review con minors abiertos). Una fila por ítem.
- `Estado` se actualiza en la fila (`abierta | pagada <fecha> | aceptada-permanente <razón>`); nada se borra.
- En Feature Ready, el planner revisa las filas `abierta` del área tocada: si una feature pasa por encima de una deuda registrada, o la paga o la re-acepta explícito.

| ID | Fecha | Origen (task/review) | Descripción | Severidad | Dueño | Estado |
|---|---|---|---|---|---|---|
| D1 | 2026-08-13 | sicop-hardening · R0 contract v1 | AI-Forge no tiene CI (`.github/workflows` ausente). La escalera de `SDD/docs/doc_quality_gates.md` sólo corre local, sin paridad en el remoto. Crear el workflow cambia la política de merge del equipo, que es decisión de Gabriel. | MAJOR | Gabriel Rojas | abierta |
| D2 | 2026-08-13 | sicop-hardening · R2 contract v1 | Los commits de agente no van firmados con GPG. La identidad `sdd-agent` previene el descuido, no a un actor decidido: cualquiera puede declarar cualquier `user.name`. | MINOR | Gabriel Rojas | abierta |
| D3 | 2026-08-13 | sicop-hardening · R2 contract v1 | El enforcement de identidad de agente nace apagado: no protege nada hasta que un repo exporte `SDD_AGENT_ENFORCE=1`. Falta decidir en qué repos se activa. | MINOR | Gabriel Rojas | abierta |
| D4 | 2026-08-13 | sicop-hardening · R0 review ronda 1 | 3 hallazgos `SC2016` (info) de shellcheck en `plugins/sdd-flow/scripts/*.sh`, preexistentes en `prod`. Son backticks literales dentro de plantillas markdown — uso correcto, no defecto. Fuerzan el piso del gate 2 a `--severity=warning` en vez del default `style`. | MINOR | Gabriel Rojas | abierta |
| D11 | 2026-08-14 | sicop-hardening · R4 review r3 | **`sdd-check.sh` se marca a sí mismo**: las dos líneas de guard que agregó AC42 contienen los literales de las regex que detectan, así que al ser líneas agregadas en el diff se matchean solas y `sdd-check.sh e5ef90c` sale 2 con dos `BLOCKER` sobre el propio script. Es transitorio a este diff —una vez mergeado, los diffs futuros no tocan esas líneas—, pero la Fase 1 del reviewer corre este script cada ciclo y un checker que se marca solo entrena a ignorar el exit 2. Fix con precedente aceptado en el repo: la auto-exclusión de `SDD/tests/secret-scan.sh:34,89`. | MINOR | Gabriel Rojas | pagada 2026-09-01 (ver RT22) |
| D10 | 2026-08-14 | sicop-hardening · R3 re-review + planner | **Las dos herramientas mecánicas del plugin se ponen rojas sobre la prosa que describe lo que prohíben.** `sdd-check.sh` levanta `BLOCKER` de supresor sobre archivos `.md` (se cierra en AC42), y `guard-git.sh` deniega cualquier comando cuyo texto contenga el literal del flag de bypass de hooks, aunque esté en prosa: rechazó dos comandos del reviewer y un mensaje de commit del planner. El fail-open no aplica porque el hook entiende el comando, sólo lo interpreta mal. Workaround actual: construir el literal en runtime. | MINOR | Gabriel Rojas | abierta |
| D9 | 2026-08-14 | sicop-hardening · R3 review r1 + R4 r1 | **Ampliado en R4**: con el arquetipo `analysis`, `evals/golden-requirements.md` queda con **diez arquetipos y nueve goldens**, y el id `G10` ya está tomado por otra cosa, así que agregar el golden faltante exige decidir la numeración. Además: `plugins/sdd-flow/evals/golden-requirements.md` quedó atrás de la regla de mutación: las propiedades universales U1-U8 no incluyen "cada AC de detección lleva su `Mutación:`", y G1/G4/G5 producen ACs de detección por definición sin que su salida esperada lo anote. Nadie consume los evals en runtime y no gatean ningún ciclo, así que un eval desactualizado degrada el auto-test del plugin sin producir un verde falso en una review real. | MINOR | Gabriel Rojas | abierta |
| D8 | 2026-08-13 | sicop-hardening · R1 re-review | **El plumbing de git no ve lo que git no conoce**, y tres artefactos del ciclo dependen de él. (a) La nota del gate 2 recomendaba verificar con `git ls-files`, que pierde hallazgos de severidad `warning` en archivos sin `git add` — corregido en `doc_quality_gates.md:27`. (b) `SDD/tests/secret-scan.sh:105` escanea `git ls-files`: un secreto en un archivo sin `git add` sale exit 0 (medido). Está **declarado** en su línea 10, y el exit 4 de R1 fuerza árbol limpio para la evidencia que se commitea, así que la ventana es angosta — pero existe entre corridas. (c) `git stash create` de `D7`, misma causa. | MINOR | Gabriel Rojas | abierta |
| D7 | 2026-08-13 | sicop-hardening · R1 review ronda 1 | `git stash create` no representa archivos sin trackear, así que el `Tree:` estampado en modo `--allow-dirty` puede omitir contenido que sí estaba presente cuando corrieron los gates. Medido: con `?? nuevo.sh` en el árbol, `git ls-tree -r <hash>` no lo devuelve. Los dos caminos por defecto hacia evidencia commiteable salen 4 sin escribir, así que el modo riesgoso es opt-in — pero el hash sigue siendo incompleto cuando se opta. | MINOR | Gabriel Rojas | abierta |
| D6 | 2026-08-13 | sicop-hardening · R0 review ronda 2 | `SDD/tests/secret-scan.sh` acota el valor al charset `[A-Za-z0-9_./+=-]` para matar el falso positivo de `sdd-agents.md:12`. Residuo medido: el valor necesita 4 caracteres del charset **al principio**, así que `password = "p@ssw0rd!"` y `SMTP_PASSWORD=$ecret123` pasan sin detectarse. Trade-off consciente contra el falso positivo; la forma con puntuación al inicio queda sin cubrir. | MINOR | Gabriel Rojas | abierta |
| D5 | 2026-08-13 | sicop-hardening · R0 review ronda 1 | La "Suite completa" de `SDD/docs/doc_quality_gates.md` es el mismo comando que el gate 4, así que `--full` lo re-ejecuta y suma un verde sin agregar cobertura. Cuando el repo tenga una suite más lenta que la escalera, separarlas. | MINOR | Gabriel Rojas | abierta |
| D16 | 2026-08-27 | consumo GEN-101 · ronda 3 del reviewer, MJ1 | El fix de MAJOR 10/M4 (`sdd-context-budget.sh` deriva forma pelada de `standards/x.md` además de la prefijada) no tiene ningún test que se ponga rojo si se borra: la otra mitad del fix (agregar el prefijo `standards/` a `reviewer-agent.md:38`) ya alcanza para que el grep prefijado, que existía desde antes, encuentre `base-standards.md`. AC27bis inyecta su referencia de prueba en forma prefijada, nunca pelada, así que nunca ejercita el código que se supone que cubre. | MAJOR | Gabriel Rojas | abierta |
| D17 | 2026-08-27 | consumo GEN-101 · ronda 3, MJ2 | `plugins/sdd-flow/agents/reviewer-agent.md:38` (agregar el prefijo `standards/` a la cita de `base-standards.md`) es parte real del fix de la subestimación de R1, y cambió durante el ciclo (aparece en `git diff --name-only 01756e5 HEAD`), pero no figura en ningún Architectural Delta de R1 ni de ningún otro requerimiento. | MAJOR | Gabriel Rojas | abierta |
| D18 | 2026-08-27 | consumo GEN-101 · ronda 3, MJ3 | El Architectural Delta de R1 en el contract dice que `sdd-context-budget.sh` es "canónico, v0.2.0 tras v7"; el script real está en `0.3.0` (bump de la propia ronda 2, declarado en la ratificación v8 pero no propagado al Delta de R1). | MAJOR | Gabriel Rojas | abierta |
| D19 | 2026-08-27 | consumo GEN-101 · ronda 3, MJ4 | La tabla del Architectural Delta de R3 tiene 4 filas; el checklist del arquetipo del mismo R3 dice "los 5 archivos del Delta", y la prosa de la baja de AC16 nombra `commands/sdd-pr.md` como "nuevo en el Delta" — pero esa fila nunca se agregó a la tabla, aunque el archivo sí está en el diff real del ciclo. | MAJOR | Gabriel Rojas | abierta |
| D20 | 2026-08-27 | consumo GEN-101 · ronda 3, MJ5 | El Architectural Delta de R3 describe el cambio a `archetypes.md` como "Fila nueva: criterio de brief trivial (4 condiciones)". El Out of scope del mismo contract ya reconoce que esa descripción fue el defecto de v6 ("una fila de tabla", contradecía la Decisión de diseño de R3) y dice que se corrigió en v7 a "una sección normativa completa" — pero el fix llegó al Out of scope y nunca volvió al Delta, que sigue describiendo lo viejo. | MAJOR | Gabriel Rojas | abierta |
| D21 | 2026-08-27 | consumo GEN-101 · ronda 3, MJ6 | `.sdd/state.json` (gitignored, no viaja en el PR, pero es la fuente que el verification report cita como evidencia de `execution_mode: inline`) quedó desactualizado desde la Fase 4 original: cita `contract_version: "v5"` con el contract en v9, cita tres paths de `SDD/verification/` que ya no existen (renombrados a `-gates.md`), sus ratificaciones registradas cortan en v5, y su array `escalations` está vacío mientras `SDD/escalations.md` tiene 8 filas, 2 de este mismo ciclo. | MAJOR | Gabriel Rojas | abierta |
| D22 | 2026-08-27 | consumo GEN-101 · ronda 3, MJ7 | `SDD/escalations.md` dice que el backfill se derivó leyendo "las 7 entradas rotuladas Historial de versiones (v2 a v8)" del contract de GEN-94. El contract real tiene 6 líneas con ese rótulo exacto (v7 a v2); v8 vive bajo el rótulo distinto `Contract version`. El total de 8 versiones (v1-v8) sigue siendo correcto — sólo el desglose de cómo se llegó a ese número está mal descrito. | MAJOR | Gabriel Rojas | abierta |
| D23 | 2026-08-27 | consumo GEN-101 · ronda 3, MJ8 | El verification report (`SDD/verification/refactor-GEN-101-optimizacion-consumo.md`) tiene una tabla de "Gates — evidencia GENERADA" que lista 4 reportes del runner y los describe como "Corrida final, post-v7" / "un quinto reporte". A esta altura hay 6 reportes (`-v7-gates.md` y `-v8-gates.md` no están en la tabla), y el que sella el árbol vigente es `-v8-gates.md`. Quien lea sólo la tabla no encuentra la evidencia del estado actual. | MAJOR | Gabriel Rojas | abierta |
| D24 | 2026-08-27 | consumo GEN-101 · ronda 3, MJ9 | Los 6 ACs de R1 (AC17, AC17bis, AC18, AC19, AC20, AC27bis) nunca se clasificaron explícitamente contra `quality-gates.md` §10.1 — a diferencia de R4 y R6, que sí tienen una frase "AC de detección: ...". AC17bis y AC27bis afirman que un recorrido de archivos coincide con un recorrido independiente, lo que encaja en la forma 3 de §10.1 (ausencia sobre un conjunto que se recorre mal), pero ninguno lleva mutación declarada. Es la causa raíz de D16: sin la clasificación, nadie exigió el triple que habría forzado el test a poder ponerse rojo. | MAJOR | Gabriel Rojas | abierta |
| D25 | 2026-08-27 | consumo GEN-101 · ronda 3, MJ10 | La Decisión de diseño (cerrada) de R3 en el contract sigue diciendo que el arquetipo elegible para brief trivial es "`refactor`, `infra` o `bugfix`". La implementación real (`plugins/sdd-flow/standards/archetypes.md`, condición 3) dice "`refactor` o `infra`" y explica por qué se sacó `bugfix` — corrección que nunca volvió al contract. `AC14` tampoco lo detecta: su test assertea las condiciones 1, 2 y 4, pero nunca la 3. | MAJOR | Gabriel Rojas | abierta |
| D26 | 2026-08-27 | consumo GEN-101 · ronda 3, minor | El Out of scope del contract dice que retrofitear una columna nueva en `SDD/retro.md` exigiría "tocar las 16 filas existentes" — al momento de escribirlo eran 16 (RT1-RT16); hoy son 21 (RT1-RT21, tras las rondas de este mismo ciclo). El argumento de fondo (no forzar clasificación retroactiva sin verificarla) se sostiene igual; sólo el número quedó viejo. | MINOR | Gabriel Rojas | abierta |
| D27 | 2026-08-27 | consumo GEN-101 · ronda 3, minor | El Out of scope del contract dice que las dos clases de defecto de plan que R4 no implementó "quedan como dos ítems de SDD/debt.md". En la práctica quedaron como una sola fila (D12) que describe las dos clases juntas. La deuda está registrada y visible; sólo el conteo de filas no coincide con la prosa. | MINOR | Gabriel Rojas | abierta |
| D28 | 2026-08-27 | consumo GEN-101 · ronda 3, minor | El verification report dice "Ninguno de los 27 ACs... está marcado manual-only. N/A" en la sección de Smoke manual, pero el binding de AC26 sí describe una verificación manual (contra la respuesta de Proxima). La evidencia de AC26 existe igual, en su propia sección — el "N/A" de Smoke manual sobra o debería puntear ahí en vez de negar que existe verificación manual en el ciclo. | MINOR | Gabriel Rojas | abierta |
| D29 | 2026-08-27 | consumo GEN-101 · ronda 3, minor | La salida pegada del triple de mutación de AC19 en el verification report es de una corrida contra `sdd-context-budget.sh` 0.2.0 (menciona `line 66`/`line 68`, números que correspondían a esa versión). El script real es 0.3.0 y ese bloque de código se movió de línea. La aserción del AC sigue siendo válida (re-verificado: sigue exit 0 vs exit 2 limpio), pero la transcripción pegada es de una versión ya superada del script. | MINOR | Gabriel Rojas | abierta |
| D30 | 2026-08-27 | consumo GEN-101 · ronda 3, minor | Dos cifras insignia de R1 ("23,6% de subestimación", la de ronda 1; "9,2% de subestimación", la de ronda 2) se citan en el contract sin la salida del cálculo pegada en ningún artefacto. El 9,2% se re-derivó y coincide (7681/83722 ≈ 9,17%); el 23,6% de la ronda 1 ya no es re-derivable de ningún artefacto vigente porque el estado que lo produjo fue corregido antes de que alguien lo transcribiera con su cálculo. | MINOR | Gabriel Rojas | abierta |
| D31 | 2026-09-08 | usage-monitor GEN-106 (hallazgo incidental, sin ronda de review) | **`SDD/tests/secret-scan.sh` no tiene guard `.md`**, a diferencia de las tres reglas built-in de `sdd-check.sh` (D10/D11, cerrado en `fix-check-self-scoping`). Medido: un verification report que cita, como evidencia, la forma exacta de un hallazgo real de `secret-scan.sh` (identificador que contiene una palabra vigilada asignado con `=` a otro identificador) se autodetecta al commitearse — mismo mecanismo de "eco creciente" que motivó el fix de `sdd-check.sh`, ahora en la herramienta hermana. Sin corregir: cualquier evidencia futura que cite un hallazgo real de este gate en prosa corre el mismo riesgo. Precedente ya aceptado en el repo para generalizar (RT13, `SDD/retro.md`): exclusión por línea con lenguaje de prohibición, no por archivo. | MINOR | Gabriel Rojas | abierta |
| D12 | 2026-08-27 | consumo GEN-101 · R4 contract v7 (hallazgo de revisión externa, MAJOR 12) | El diseño original de R4 (`docs/specs/2026-08-25-consumption-optimization-design.md`) nombraba tres clases de defecto de plan a atacar: secciones obligatorias ausentes (implementado), AC que cruza su propio out-of-scope (self-review de `sdd-plan` contra el Delta) y Delta ubicado por vecindad en vez de por condición. Sólo la primera se implementó. La baja de scope no se declaró en ningún lado hasta que revisión externa la encontró. Las otras dos siguen sin mecanismo — un contract puede seguir teniendo un AC que cruza su propio scope (RT2) o un Delta ubicado por "tras X" (RT9) sin que nada lo detecte antes del review humano. | MAJOR | Gabriel Rojas | abierta |
| D13 | 2026-08-27 | consumo GEN-101 · R2 contract v5 (auto-declarado por el planner) | El chequeo de versiones pinneadas de AC10 excluye por línea las que llevan lenguaje de prohibición (`defecto`, `prohibi`, `nunca versión`, `envejece`) para no marcarse a sí mismo (clase D10/D11). Límite declarado en su momento: una violación real escrita en la MISMA línea que ese lenguaje de prohibición se pierde. Angosto — el patrón de la línea es prosa normativa, no código que alguien vaya a escribir por accidente junto a una versión pinneada — pero no se corrige, es un trade-off consciente. | MINOR | Gabriel Rojas | abierta |
| D14 | 2026-08-27 | consumo GEN-101 · R4 contract v7 (hallazgo de revisión externa, MINOR 19b, corregido) | El fix del strip-prefix de `concerns:` en `sdd-lint-contract.sh` (v0.12.0) agregó `\|` y `>` a los caracteres pelados, cubriendo blockquote y fila de tabla — las dos formas que la fixture del reviewer encontró. Es la misma clase que D10/D11 (D6, en rigor): un enfoque enumerativo por caracteres puede volver a perder una forma nueva no anticipada (ej. una lista numerada `1. **Concerns**: ...`, o HTML embebido). No se generaliza a un parser de markdown real — sería una dependencia nueva para un chequeo de presencia. | MINOR | Gabriel Rojas | abierta |
| D15 | 2026-08-27 | consumo GEN-101 · R6 contract v6 (hallazgo de revisión externa, MINOR 19c) | `sdd-escalation-tally.sh` lee la columna `Clase` posicionalmente (`awk -F'\|' '{print $6}'`). Un `\|` literal dentro del texto libre de la columna `Evento` corre el campo y el conteo sale mal — falla ruidoso (probablemente exit 3, `INVALIDA`, no un subconteo silencioso), así que es molestia operativa, no el defecto de medición silenciosa que este mismo ciclo viene cazando. No se corrige acá: requeriría escapar `\|` en la columna `Evento` al escribir cada fila, y las 6 filas backfilleadas de v7 ya no lo tienen. | MINOR | Gabriel Rojas | abierta |
| D32 | 2026-09-18 | bisalta-db GEN-108 · R1 ronda 3, MINOR del reviewer | El addendum de rondas 1-2 del verification report se restauró desde `fd9447e` y no desde `bb592b1`, que era la versión más reciente. La entrada 5 perdió el nombre del commit, la enumeración de gates y el puntero al tree hash, y quedó como "corrida final del runner de arriba" — pero ese encabezado ya es el de ronda 3. Verificado: 188 de 189 líneas idénticas byte a byte, el triple AC9 intacto, y el texto es literal de un commit real, no escrito de memoria. | MINOR | AGENT_r1 / planner | abierta |
| D33 | 2026-09-18 | bisalta-db GEN-108 · R1 ronda 3, MINOR del reviewer | El triple AC9 del addendum de rondas 1-2 cita `postgres-parte-a.sql:54` sobre un árbol donde el archivo tenía 53 líneas. Ronda 3 lo llevó a 58, así que sobre el árbol actual la mutación cae en la línea 59 — el reviewer lo verificó corriendo el triple de cero. La cita es correcta para su propio árbol y quedó rancia respecto del actual. Clase RT20 dentro de evidencia histórica; la propiedad de fondo se sostiene. | MINOR | AGENT_r1 | abierta |
| D34 | 2026-09-18 | bisalta-db GEN-108 · R1 ronda 3, ADVISORY del reviewer — **limitación del plugin, no del trabajo** | `sdd-run-gates.sh:272` escribe `} > "$OUT"` (redirección truncante), mientras `quality-gates.md` §10.3 manda pegar los triples de mutación **a mano en ese mismo archivo**, y el encabezado que el runner estampa declara que editarlo a mano invalida la evidencia. Las tres cosas no pueden ser verdad a la vez: re-correr la escalera después de pegar un triple lo destruye, que es justo lo que pasa en cada ronda de review. `AGENT_r1` pisó la trampa, la detectó, la declaró y se recuperó. | MAJOR | sdd-flow (otro ciclo) | abierta |
| D35 | 2026-09-18 | bisalta-db GEN-108 · R2 ronda 1, BLOCKER | **El verification report queda fuera del gate que valida el árbol.** El runner sella el árbol y *después* se pega el addendum manuscrito y se commitea, así que el archivo de evidencia nunca pasa por el gate 9. Medido: el árbol sellado no contenía el report, y el árbol entregado tenía `secret-scan.sh` en exit 1 por dos literales del addendum. Es la consecuencia operativa de `D34`, y es más grave: un secreto pegado en la evidencia llega a `prod` sin que ningún gate lo vea. Mitigado en este ciclo corriendo el scan sobre el árbol final post-addendum y declarándolo; sin mecanismo todavía. | MAJOR | sdd-flow (otro ciclo) | abierta |
| D36 | 2026-09-18 | bisalta-db GEN-108 · R2 ronda 2, MINOR | `verification/…-R2.md:243` justifica dejar dos citas `contract v2` diciendo que la versión corriente de `2026-08-13-sicop-hardening.md` "sí es v2"; ese contract está en **v8**. La decisión de no propagarlas es correcta, pero por otra razón: son marcadores históricos de ratificación (AC3 ratificado en v2, threat model agregado en v2). Bajo la razón escrita, alguien las "corregiría" a v8 y se equivocaría. | MINOR | AGENT_r2 | abierta |
| D37 | 2026-09-18 | bisalta-db GEN-108 · R2 ronda 2, MINOR | `verification/…-R2.md:514` cita `sha256:2478fcac35a40aaf` como el hash del doc estampado arriba; lo estampado es `56736c3e5b778ea1`, que además es el hash real en `HEAD`. El valor citado es el de la ronda 1. La conclusión que sostiene es cierta; la cifra no. **Quinta instancia de la clase de drift**, con el valor viejo grepeable en el árbol — RT20 debió atraparlo. | MINOR | AGENT_r2 | abierta |
| D38 | 2026-09-18 | bisalta-db GEN-108 · R2 ronda 2, MINOR | `verification/…-R2.md:293` y `:473` rotulan AC34 como "parcialmente manual", contra el contract v3 punto 3, que cierra que AC34 **no** es `manual-only` y que el handshake real no es AC34 sino el riesgo de `protocolVersion`. La prosa ya es consistente con v3; sólo el rótulo quedó en el encuadre de v2. | MINOR | AGENT_r2 | abierta |
| D39 | 2026-09-18 | bisalta-db GEN-108 · R1 ronda 5, MINOR | `SDD/briefs/R1-infra-accesos-lectura.md` declara "cuatro coincidencias" para un grep de `32 bases` sobre `plugins/bisalta-db/`; la corrida real devuelve **8** líneas. Cada ocurrencia es correcta; el conteo no reproduce. Sexta instancia de la clase de drift de cifras. | MINOR | AGENT_r1 | abierta |
| D40 | 2026-09-18 | bisalta-db GEN-108 · R1 ronda 5, MINOR (defecto preexistente de la ronda 3) | `RUNBOOK.md` dice "correrlo de nuevo contra la instancia de prueba para dejarla limpia", pero re-correr la parte B restaurada **no** quita `bisalta_lectura` de `master`/`model`/`msdb`/`tempdb`, y `sqlserver-inverso.sql` tampoco los alcanza (`database_id > 4`). La instancia de prueba queda sucia contra lo que el paso declara. | MINOR | AGENT_r1 | **cerrada en v10** — el inverso pasó a enumerar `sys.databases` completo, bases de sistema incluidas, así que la consecuencia registrada ya no ocurre |
| D41 | 2026-09-18 | bisalta-db GEN-108 · R1 ronda 5, MINOR | `postgres-parte-0.sql` afirma que `has_database_privilege(…,'CONNECT')` "sale true para cualquier rol en cualquier base". Deja de ser cierto si se toma el camino (b) del contract v6 punto 6 (`REVOKE CONNECT … FROM PUBLIC`), donde esa columna pasa a ser justamente el discriminador. Falta acotar con "mientras `CONNECT` no se haya revocado de PUBLIC". | MINOR | AGENT_r1 | abierta |
| D42 | 2026-09-21 | bisalta-db GEN-108 · medición de Patrick Ocampo | `proveedores_dev` y `proveedores_qa` tienen como dueño la cuenta personal `ian.vargas`. Si esa cuenta se rota o se va, las tablas quedan huérfanas — el mismo problema del `ALTER DEFAULT PRIVILEGES FOR ROLE`. Hay dos casos más en `sistemas-costruplaza-db`. **Riesgo aceptado y diferido por Ian Vargas (21-sep-2026)**: un superadmin puede mover el propietario si llega a pasar. No bloquea el ciclo. **Creció el 22-sep-2026** (contract v15): al conceder `CREATE ON SCHEMA public` explícito antes de revocarlo de `PUBLIC`, Patrick Ocampo tuvo que nombrar al dueño actual de `proveedores_dev` y `proveedores_qa` — `ian.vargas` —, así que el día que la propiedad pase a un rol de servicio **no alcanza con cambiar el dueño: hay que mover también ese grant**. Eran un lugar; ahora son dos. | MINOR | Ian Vargas | abierta |
| D43 | 2026-09-21 | bisalta-db GEN-108 · contract v8 | **La cuenta de AWS de producción no está enumerada.** Se sabe que `sistemas-construplaza-db` tiene 12 bases, pero `cr4rbgr7qlr6` es sufijo de cuenta y la cuenta puede tener más clusters que nadie miró. El out-of-scope del contract la excluye entera, así que no hay exposición — pero la afirmación "producción es irrepresentable" descansa en que ninguna entrada del catálogo apunte ahí, no en un inventario. | MAJOR | sin dueño | abierta |
| D44 | 2026-09-21 | bisalta-db GEN-108 · contract v8 | **Dos clusters de la cuenta de dev siguen sin medir**: `dev-costruplaza-db` y `erpodoo-19-dev`, este último **con IP pública**. Ninguno está en el catálogo, así que no hay exposición hoy; se registra porque el alcance "lectura en las bases de dev" es incompleto mientras no se midan. | MAJOR | Patrick Ocampo | abierta |
| D45 | 2026-09-21 | bisalta-db GEN-108 · contract v9 | **La lista de 6 bases de Postgres es catálogo, no permiso.** `pg_read_all_data` es membresía de cluster: nombrar seis entradas no restringe al rol a esas seis. El rol alcanza toda base donde `CONNECT` siga concedido a PUBLIC — hoy **26 de las 29**. En SQL Server la lista sí es real (los grants son por base). Cerrarla exigiría `REVOKE CONNECT … FROM PUBLIC` sobre las 20 restantes (afecta a todos los roles del cluster) o cambiar ese cluster a `GRANT SELECT` por base (revierte la decisión razonada de Patrick). Declarado en el contract, no disimulado. | MAJOR | Patrick Ocampo / Ian Vargas | abierta |
| D46 | 2026-09-21 | bisalta-db GEN-108 · segunda instancia de RT31 | `test_servidor_mcp.sh` congelaba el nombre de conexión `dev-sql` y se puso rojo el día que el catálogo pasó de una entrada por instancia a una por base — un cambio de datos legítimo. Corregido derivando el nombre del catálogo, con mutación verificada (cambiar el host pone 2 asserts en rojo). **Es la segunda vez en este ciclo** que un test congela un valor que vive en un archivo de datos; la primera fue el total del ledger de escalaciones. Candidato a chequeo del harness: ningún `test_*.sh` debería contener literales que existan en `catalogo.json` o en un ledger. | MINOR | sin dueño | abierta |
| D47 | 2026-09-21 | bisalta-db GEN-108 · R1 ronda 3, MINOR del reviewer | El bloque "Estado de arranque de la instancia de prueba" de `RUNBOOK.md` declara dos precondiciones (sin user preexistente, login preexistente) y **no declara qué bases tiene que contener la instancia**. El verde de cierre de AC7 (`tiene_user = 1` sólo en las de `@bases_permitidas`) y el rojo de la mutación presuponen que las seis bases de la lista existen ahí y que hay al menos una base de usuario fuera de ella. En una instancia sin esas bases, el script imprime `AUSENTE` seis veces y el cierre sale todo en `0` — **satisface la frase sólo vacuamente**. | MINOR | AGENT_r1 | abierta |
| D48 | 2026-09-21 | bisalta-db GEN-108 · contract v10, decisión de Patrick Ocampo | **Sale `db_denydatawriter`.** Cada base concedida lleva `db_datareader` y nada más. Lo que se pierde, escrito por pedido explícito de quien decidió: sin `db_denydatawriter` **no queda un `DENY` explícito**, así que un `GRANT` de escritura concedido por error en el futuro no tendría nada que lo anule. En SQL Server el rol pasa a ser la única barrera, literalmente. Aceptado por Patrick Ocampo. | MAJOR | Patrick Ocampo | aceptada |
| D49 | 2026-09-21 | bisalta-db GEN-108 · hallazgo de Patrick Ocampo (es de Platform, no de este ciclo) | **`sistemas-costruplaza-db` no exporta logs a CloudWatch** (`EnabledCloudwatchLogsExports` en `null`, sin grupo de logs). Sobre ninguna de sus bases se puede contestar después *"quién se conectó"* o *"esto falló"*. Acota el bloque `observability` de este contract: `application_name` sigue permitiendo que `pg_stat_activity` atribuya **en el instante**, pero no hay registro durable del lado del motor — la bitácora local del servidor MCP es el único, y es del lado del cliente. Patrick se lo pasa a Moreno junto con la retención de los 16 clones. | MAJOR | Platform & Infrastructure (Moreno) | abierta |
| D50 | 2026-09-21 | bisalta-db GEN-108 · R1 ronda 3 de v11, MINOR del reviewer | `SDD/verification/…-R1.md` afirma que *"tres corridas seguidas del mismo comando sin `sort` devolvieron las mismas ocho rutas en tres órdenes distintos"* — una cifra sin la salida que la produce pegada. La conclusión que sostiene (las ocho rutas) el reviewer la re-derivó y es correcta; lo que falta es la evidencia de las tres corridas. | MINOR | AGENT_r1 | abierta |
| D51 | 2026-09-22 | bisalta-db GEN-108 · pregunta de Ian Vargas | **El `secret_id` de las 12 entradas del catálogo lo inventó el planner y nadie lo acordó.** Tres huecos en uno: (a) los nombres salieron del borrador de v1 y Patrick nunca los vio — y no siguen la convención que la cuenta ya usa; (b) el catálogo tiene **2 secretos para 12 conexiones**, contra la regla de Patrick *"una conexión, un secreto — nada de un secreto compartido"*, aunque técnicamente hay un solo login por motor; (c) `neo_lectura` está en el runbook y **ningún artefacto del plugin lo consume**. Preguntado a Patrick el 22-sep-2026. **(a) y (b) CERRADOS el mismo día**: eligió `dev/bd/claude-lectura-postgres` y `dev/bd/claude-lectura-sqlserver` —que entran en las dos convenciones que la cuenta ya usa— y reformuló su propia regla (*"un secreto por credencial, y el aislamiento por política IAM por consumidor"*), bajo la cual el esquema de 12 conexiones sobre 2 secretos es correcto. **(c) CERRADO el 22-sep-2026**, y con el supuesto invalidado: NEO **no abre ninguna conexión Postgres** —ni hoy ni en el diseño de NEO QA— y lee Odoo stg por XML-RPC, contra la aplicación. Además corre en la cuenta de **producción**, no en la de dev. Decisión: **sale `neo_lectura`, queda un solo rol**. | MAJOR | Patrick Ocampo | **cerrada** |
| D52 | 2026-09-22 | bisalta-db GEN-108 · tercera instancia de RT31/RT39 | `test_servidor_mcp.sh` congelaba el `secret_id` literal y se puso rojo el día que Patrick eligió los nombres reales — un cambio de datos legítimo. **Tercera vez en este ciclo** que un test congela un valor que vive en un archivo de datos: antes fueron el total del ledger de escalaciones y el nombre de conexión `dev-sql`. Corregido derivando del catálogo, con mutación verificada. El patrón ya no es anécdota: **candidato a chequeo del harness** que prohíba en `SDD/tests/` literales presentes en `catalogo.json` o en un ledger. | MINOR | sin dueño | abierta |
| D53 | 2026-09-22 | bisalta-db GEN-108 · contract v13, decisión de Patrick Ocampo | **Se resigna que `pg_stat_activity` distinga consumidores.** Al quedar un solo rol (`claude_lectura`), el día que haya un segundo consumidor no se va a poder saber quién corrió qué desde el lado del motor. Hoy el único consumidor es Ian Vargas, así que no se pierde nada que exista. Remedio cuando haga falta: agregar el rol a la lista de la Parte A y volver a correrla — el script es idempotente. Aceptado por Patrick, que lo pesó contra *"un rol sin consumidor es una clave que rotar, una cuenta que olvidar, y sobre todo una pista falsa de que los dos mundos están conectados"*. | MINOR | Patrick Ocampo | aceptada |
| D54 | 2026-09-22 | bisalta-db GEN-108 · R1 ronda 1 de v14, MINOR del reviewer | El barrido de `neo` se declaró *"por concepto sobre toda la entrega"* pero el comando se acotó a `plugins/bisalta-db/`, y se le escapó `CHANGELOG.md`, que seguía diciendo *"desde Claude Code y NEO"*. **Cuarta instancia de la clase de alcance de barrido** (`RT32` universo, `RT37` directorio, `RT38` patrón, y ahora otra vez directorio pese a la instrucción explícita). Corregido por el planner; el agente no podía editar ese archivo, pero sí reportar el hallazgo. | MINOR | AGENT_r1 | corregida |
| D55 | 2026-09-22 | bisalta-db GEN-108 · R1 ronda 1 de v14, MINOR del reviewer | El párrafo del `contract-change-request` en `RUNBOOK.md` sigue describiendo el texto de `AC1` como vigente en v13, cuando v14 ya lo resolvió. Queda como histórico sin marcar. | MINOR | AGENT_r1 | abierta |
| D56 | 2026-09-22 | bisalta-db GEN-108 · primera prueba con el secreto real | **`/plugin install` desde un marketplace local COPIA el plugin, no lo enlaza al árbol de trabajo.** Medido: se cambió el `secret_id` de las 12 entradas en el repo y el plugin instalado siguió pidiendo el identificador viejo, fallando con `secreto_inaccesible` — que se lee como un problema de permisos y no lo era. Consecuencia: **`secreto_inaccesible` tiene ahora tres causas que se ven igual** — falta el permiso IAM, la forma del secreto no es la esperada, o el catálogo instalado está viejo. Documentado en el README del plugin con el comando para comparar los dos catálogos. **Ampliado el 23-sep-2026**: reinstalar no alcanza. El servidor carga su código al arrancar y relee el catálogo en cada llamada, así que una sesión abierta queda con código viejo leyendo datos nuevos: un cambio de **valores** se toma solo, uno de **forma** rompe la sesión con `catalogo_invalido` (falla cerrada), y uno de **código** no se toma y no avisa. Cada sesión tiene su propio proceso. Y `remove` no borra la carpeta del cache. Documentado en el README con la tabla de los tres casos y el comando para ver qué procesos quedaron atrás. | MINOR | documentado | corregida |
| D57 | 2026-09-22 | bisalta-db GEN-108 · primera prueba con el secreto real | **El diagnóstico que discrimina las causas de un fallo de conexión no está versionado.** Existe y funciona —descarta binarios, catálogo instalado vs repo (D56), identidad AWS, forma del secreto, DNS, endpoint de réplica, puerto, y clasifica la salida real de `psql`— pero vive en el scratchpad de una sesión, así que el próximo que tenga el mismo error vuelve a armarlo o vuelve a leer mal `secreto_inaccesible`. Es la respuesta operativa a D56 y a las tres causas indistinguibles que ese ítem registró. No se commiteó porque el contract de GEN-108 no lo pide: entrarlo al PR es alcance nuevo, y la decisión es del planner, no del ciclo en curso. | MINOR | Ian Vargas | abierta |
| D58 | 2026-09-23 | bisalta-db GEN-108 · contract v16 (mediciones del 23-sep) | **El plugin anula el `statement_timeout` que Patrick fijó en el rol.** `ALTER ROLE claude_lectura SET statement_timeout=60s`, pero el plugin manda `statement_timeout=120000` por `PGOPTIONS` (AC28) y lo que manda el cliente le gana a lo configurado por rol: medido el 23-sep, rige `2min`. AC28 se escribió sin saber que existía el de 60 s. Decidir cuál manda es de Patrick: bajar AC28 a 60 s, que el plugin deje de mandarlo en Postgres, o mantener 120 s a sabiendas. | MINOR | Patrick Ocampo | pagada 2026-09-23 — decisión de Patrick: el plugin deja de mandar `statement_timeout` y rige el del rol (contract v19, `AC28`) |
| D59 | 2026-09-23 | bisalta-db GEN-108 · contract v16 (mediciones del 23-sep) | **El `ALTER ROLE` que Patrick aplicó no está en `postgres-parte-a.sql`.** El rol en dev tiene `default_transaction_read_only=on`, `statement_timeout=60s`, `idle_in_transaction_session_timeout=30s` y `lock_timeout=5s` fijados por rol; el script del repo sólo hace `CREATE ROLE` y `GRANT pg_read_all_data`. Correr la Parte A en otro cluster, o recrear el rol, deja afuera esa capa — y la medición del 23-sep mostró que es la barrera que frenó la sonda 3 (writer sin la sesión read-only del plugin). Es alcance de R1: se reabre con su propio AC y su mutación. | MAJOR | Patrick Ocampo / Ian Vargas | pagada 2026-09-24 — el `ALTER ROLE` entró a `postgres-parte-a.sql` en `b5e9239` (contract v19, `AC49`), y la configuración del rol quedó verificada leyendo el catálogo (v21) |
| D60 | 2026-09-23 | bisalta-db GEN-108 · contract v16 (mediciones del 23-sep) | **Con varias sentencias, el plugin devuelve sólo el resultado de la última — y depende de la versión de psql.** AC19 acepta tres sentencias de lectura seguidas; medido el 23-sep con psql 14.18, la respuesta trae sólo la tercera y descarta las dos primeras sin avisar. psql 15 o posterior devuelve todas, concatenadas con un encabezado por resultado, que el parser CSV del plugin no espera. El comportamiento de una misma consulta cambia según el cliente instalado en la máquina. | MAJOR | Ian Vargas | abierta |
| D61 | 2026-09-23 | bisalta-db GEN-108 · contract v16 (mediciones del 23-sep) | **La verificación de Patrick sobre el aprovisionamiento de Postgres vive en Slack, no en el repo.** 431 objetos legibles, cero escribibles, `CREATE TABLE` rechazado, `default_transaction_read_only` en `on` (22-sep). Es la evidencia de los ACs `manual-only` de R1 para Postgres, y hoy un traspaso no la encontraría. Pedirle la salida literal y pegarla en el verification report de R1. **Ampliado en v17**: también vive en Slack la evidencia de SQL Server del 23-sep — `MachineName` medido (`AC44`), el login verificado como el login (lee, `CREATE TABLE` → `Msg 262`, sin acceso a `SSISDB` ni `CONSTRUPLAZA_EFLOW`) y el par antes/después de `AC48` (36 nombres de base antes del `DENY`, `master` y `tempdb` después). Lo que falta en todos los casos es la **salida literal**, no la cifra reportada. | MINOR | Ian Vargas | abierta |
| D62 | 2026-09-23 | bisalta-db GEN-108 · review §7.5 ronda 2 (`ESCALATE`) | **La revisión adversarial de `AC47` no se hizo.** La ronda 2 verificó los casos que el contract enumera y las tres mutaciones declaradas, no un intento de romper la lista blanca con formas que el contract no enumera — que es exactamente cómo apareció el hueco de `RT50`. Sin esa revisión, `AC47` está probado contra lo que alguien pensó, no contra lo que alguien intentó. En SQL Server el rol es la única barrera detrás, y el login ya existe (23-sep). Se le pide a Patrick Ocampo, que administra las dos instancias. | MAJOR | Ian Vargas (se le pide a Patrick Ocampo) | pagada 2026-09-23 — revisión adversarial hecha por Patrick: doce formas de pasar la lista blanca, ninguna brecha; dos se arreglan (`AC51`, `AC52`) y dos quedan como límite conocido (contract v19). Reescrita a pedido de Patrick: la lista blanca es la capa que da un error temprano, no la que impide el daño |
| D63 | 2026-09-24 | bisalta-db GEN-108 · review §7.5 de v19 a v22, ronda 3 (`APPROVED`) | **Mutación (b) de `AC51` abierta.** Apagada la regla de los literales `E'…'` de Postgres, la suite sigue verde: la regla está implementada y los 42 casos coinciden, pero ningún caso la mata. Se le pidió a Patrick el caso que falta (Slack, 24-sep). **Cierre (24-sep, contract v23)**: la regla no está para rechazar ataques —los rechaza también la de construcción sin cerrar— sino para no rechazar consultas legítimas; Patrick mandó seis de esas (`76f258e`), y con la regla apagada caen cinco (índices 21 a 25; el 26 es control). Evidencia en el report de v23. | MAJOR | Patrick Ocampo | pagada 2026-09-24 — seis casos de Patrick (`76f258e`); con la regla apagada caen los casos 21 a 25 (contract v23, `AC51`) |
| D64 | 2026-09-24 | bisalta-db GEN-108 · review §7.5 de v19 a v22, ronda 3 (`APPROVED`) | **Mutación (c) de `AC51` abierta en parte.** Con la regla de los corchetes apagada cae un solo caso, que también cae con la mutación (d). Ningún caso cubre por sí solo esa regla. | MINOR | Patrick Ocampo | pagada 2026-09-24 — el planner escribió cuatro casos legítimos con corchetes más un control (`a282e3a`, contract v24); con la regla apagada caen los cuatro y ninguno cae con la (d) |
| D65 | 2026-09-24 | bisalta-db GEN-108 · review §7.5 de v19 a v22, ronda 3 (`APPROVED`) | **Evidencia de las mutaciones de `AC51` sobre el árbol de v22, pendiente.** Rehacerla es trabajo sobre la lista blanca que el filtro de seguridad corta cuando lo hace un agente (`RT54`). Hoy la evidencia de mutaciones es la del report de v20, corrida sobre aquel árbol. La tiene que rehacer una persona (report de v22, §3). **Avance (24-sep, v23)**: (b), (c) y (d) se corrieron sobre `76f258e` con el script del report de v20 y dos arreglos —el conteo con `grep -c '^  FAIL'` y la lista de los asserts que caen—, y el filtro no las cortó (report de v23, §2). Falta la (a), con adaptador. | MAJOR | Ian Vargas / Patrick Ocampo | pagada 2026-09-24 — la (a) se corrió con el adaptador sobre el árbol de `b684ac9` y cae; la corrió el planner, y el filtro no la cortó (addendum de `SDD/verification/feat-GEN-108-mcp-bisalta-db-v23-D67.md`) |
| D66 | 2026-09-24 | bisalta-db GEN-108 · review §7.5 de v19 a v22, ronda 3 (`APPROVED`) | **Triple del test del tipo en el motivo sin su script.** El report de v22 muestra el resultado resumido pero no el script que aplicó y restauró la mutación. El contract no exige ese triple (review de v21 y v22, ronda 3, MINOR 1). | MINOR | Ian Vargas | abierta |
| D67 | 2026-09-24 | bisalta-db GEN-108 · review §7.5 de v19 a v22, ronda 3 (`APPROVED`) | **Un caso de construcción sin cerrar sólo verifica el prefijo del motivo**, porque su descripción en el archivo de Patrick no nombra el tipo. Declarado en el binding. Cuando Patrick mande el caso de D63, pedirle que esa descripción nombre el tipo (ronda 3, MINOR 2). **24-sep**: Patrick mandó los casos de D63 (`76f258e`) y el pedido **todavía no se le hizo**; va en la próxima respuesta que se le mande. | MINOR | Patrick Ocampo | pagada 2026-09-24 — no se esperó a Patrick: Ian leyó la consulta del caso 13 y el planner le agregó el tipo (`literal`) a la descripción en `e0a70bb`, sin tocar la consulta; el tipo sale de la consulta, no del validador (report de v23, §4) |
| D68 | 2026-09-24 | bisalta-db GEN-108 · review §7.5 de la mutación (a), ronda 1 (`APPROVED`), ADVISORY 2 | **El total de `SDD/escalations.md` está congelado.** La línea "Total en este repo" dice 8 eventos, y `sdd-escalation-tally.sh` cuenta 20. El propio archivo dice que la cifra no se re-deriva a mano. Viene de antes de este ciclo. Arreglo: reemplazar la cifra por un puntero al script. | MINOR | Ian Vargas | abierta |
| D69 | 2026-09-24 | bisalta-db GEN-108 · review §7.5 de la mutación (a), ronda 3 (`APPROVED`) | **`SDD/FEATURE-READY-GEN-108.md:5` invierte la cronología.** La cláusula "la (a) se corrió después, sobre el árbol actual" quedó antes de la review de v23 y de `D67`, cuando la (a) es posterior a las dos (`7b1f9ac`). El paso 3 del brief lo dice bien. No se corrigió porque era la ronda 3. Arreglo: mover la cláusula al final de la oración que termina en `E20`. Esta fila tampoco entra en la cifra de deuda del brief, que dice 37 ítems y 25 abiertos, y con ella son 38 y 26. | MINOR | Ian Vargas | pagada 2026-09-24 — la oración se reordenó en v24, y la cifra del brief se re-derivó |
| D70 | 2026-09-25 | bisalta-db GEN-108 · contract v25, review de gradiel12 en el PR #14 | **Postgres cifra pero no verifica el certificado del servidor.** `AC55` fija `PGSSLMODE=require`. Pasar a `verify-full` necesita el bundle de certificados de RDS dentro del plugin, apuntado con `sslrootcert`. Hay que descargarlo del sitio de AWS, y eso requiere el OK de Ian. Antes de habilitar al equipo. | MAJOR | Ian Vargas | abierta |
| D71 | 2026-09-25 | bisalta-db GEN-108 · contract v25, review de gradiel12 en el PR #14 | **SQL Server cifra pero no verifica el certificado del servidor.** `AC55` usa `-N true -C`. Medido el 25-sep: sin `-C`, la conexión falla porque el certificado de la instancia no es de una autoridad conocida. Cerrarlo requiere que Patrick instale en la instancia un certificado verificable, o que distribuya su CA para que el plugin la confíe. Antes de habilitar al equipo. | MAJOR | Patrick Ocampo | abierta |
| D72 | 2026-09-25 | bisalta-db GEN-108 · contract v25, review de gradiel12 en el PR #14 | **`COMPRAS_STG` está catalogada como `ambiente: dev`.** El catálogo sólo admite `dev` y `qa`. Si es staging, la etiqueta confunde, justo en la regla de "producción irrepresentable". Hay que preguntarle a Patrick qué es esa base y cómo se etiqueta (review de gradiel12, menor 3). | MINOR | Patrick Ocampo | abierta |
| D73 | 2026-09-25 | bisalta-db GEN-108 · contract v25, review de gradiel12 en el PR #14 | **El rol `claude_lectura` no tiene tope de conexiones.** Con todo el equipo usando el plugin, muchas consultas pesadas a la vez van a la única réplica del cluster. Arreglo: `ALTER ROLE claude_lectura CONNECTION LIMIT n`, con el número que elija Patrick. Antes de habilitar al equipo. | MAJOR | Patrick Ocampo | abierta |
| D74 | 2026-09-25 | bisalta-db GEN-108 · contract v25, review de gradiel12 en el PR #14 | **Desde el motor no se sabe qué persona corrió una consulta.** Todos entran con el mismo rol y el mismo login, y `application_name` es el usuario del secreto (`AC29`, `D53`). Con una persona no importaba; con el equipo, sí. Hay que decidir si `application_name` lleva también el usuario local de quien consulta. Lo vemos desde su máquina, así que se puede falsear, pero alcanza para diagnosticar. Cambia `AC29`. Antes de habilitar al equipo. | MAJOR | Ian Vargas | abierta |
| D75 | 2026-09-25 | bisalta-db GEN-108 · contract v25, review de gradiel12 en el PR #14 | **En SQL Server, un `SELECT` puede bloquear a quien escribe.** Con el aislamiento por omisión, el `SELECT` toma bloqueos compartidos. Si las bases de Dev SQL no tienen `READ_COMMITTED_SNAPSHOT`, una consulta larga del plugin puede frenar a otros sistemas. Además, no hay techo de recursos para el login. Hay que preguntarle a Patrick por las dos cosas. Antes de habilitar al equipo. | MAJOR | Patrick Ocampo | abierta |
| D76 | 2026-09-25 | bisalta-db GEN-108 · contract v25, review de gradiel12 en el PR #14 | **`AC54` no se midió contra el motor.** Que `sqlcmd -t 60` corte a los 60 s en la instancia real no se probó, para no cargar una copia de producción con una consulta de un minuto. El harness sí verifica que la bandera viaja y que el corte se informa como `tiempo_agotado`. Se puede medir con una consulta liviana que espere más de 60 s, pero `WAITFOR` ahora se rechaza (`AC53`): hace falta otra forma de medirlo. | MINOR | Ian Vargas | abierta |
| D77 | 2026-09-25 | bisalta-db GEN-108 · review §7.5 de v25, ronda 1, ADVISORY | **El negativo de `AC25` en el camino de error no puede fallar.** El stub de `psql` nunca emite la credencial en un error, así que el assert de que no aparece se cumple solo. Viola la regla del propio test (línea 12): cada negativo va con su control positivo. Viene de antes de v25. v25 sumó ese control sólo al camino de `sqlcmd` (`AC57`). Arreglo: un modo del stub de `psql` que falle con la credencial en stderr. | MINOR | Ian Vargas | abierta |
| D78 | 2026-09-25 | bisalta-db GEN-108 · review §7.5 de v25, ronda 1, ADVISORY | **`AC53` no enumera `CHECKPOINT`, `RECONFIGURE`, `SETUSER`, `OPENXML`, `IF` ni `RAISERROR`.** Hoy se aceptan encadenados detrás de un `SELECT`. Los que importan los frena el privilegio del login, y la review de gradiel12 no los pedía. `IF` es el único que puede rechazar de más. Decidir si se suman. | MINOR | Ian Vargas | abierta |
